-- ============================================================
-- الترحيل 003b: حذف سياسة kpis قديمة متعارضة
-- ============================================================
-- التاريخ: 2026-06-06
-- السياسة المحذوفة: allow_all_kpis ON kpis
-- السبب: وُجدت في قاعدة البيانات الحية ولم تكن موثّقة،
--        وجودها مع kpis_school يُلغي العزل (OR logic)
-- ============================================================

DROP POLICY IF EXISTS "allow_all_kpis" ON kpis;

-- التحقق: يجب أن تبقى kpis_school فقط
SELECT tablename, policyname, cmd
FROM pg_policies
WHERE tablename = 'kpis';
