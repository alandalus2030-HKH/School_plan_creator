# 🧪 دليل الاختبار الشامل قبل الإطلاق

> **الموقع:** school-plan-creator.vercel.app · **آخر تحديث:** 2026-09-09
> ضع ✅ بعد نجاح البند، و❌ + ملاحظة في «سجل المشاكل» (آخر الملف) عند أي خلل.
>
> **حالة التنفيذ (2026-09-09):** اجتاز المستخدم بنود الأقسام 1–17 على **الخادم المحلي** `npm run dev`
> (قاعدة Supabase السحابية نفسها، فالعزل وRLS والترحيلات تحقّقت فعلياً). البنود المعلَّمة ⏳ يتعذّر
> التحقق منها محلياً وتنتظر تمريرة على الإنتاج بعد النشر.
> اختبر بثلاثة أدوار على الأقل: **مكلَّف** (موظف) · **مقيّم/منسق** · **مدير المدرسة/مشرف النظام**.
> الرمز بجانب البند يدل على الدور المناسب: 👤 مكلَّف · 🧑‍⚖️ مقيّم · 👑 مدير/مشرف.

---

## 0) تجهيزات الاختبار
- [x] جهّز 3 حسابات: موظف عادي (مكلَّف)، منسق/مقيّم، مدير مدرسة. (مشرف النظام اختياري للعزل.)
- [x] جهّز خطة تجريبية واحدة على الأقل (يُفضَّل هيكل رباعي).
- [x] افتح المتصفح وسجّل الدخول بكل دور في نافذة/متصفح منفصل. **(نُفّذت الجولة على الخادم المحلي `localhost:3000` المتصل بقاعدة Supabase نفسها — تمريرة الإنتاج ما زالت مطلوبة، انظر القسم 18.)**
- [x] جرّب على متصفح حديث (Chrome/Edge) + جهاز موبايل أو نافذة ~375px.

## 0.1) فحوص آلية (مُنجزة)
- [x] مدقّق أمان Supabase — لا أخطاء (ERROR). (ترحيلان 044/045)
- [x] فحص الترحيلات في الإنتاج (2026-06-19): كل الترحيلات 021–045 مطبَّقة · pg_cron نشط.
- [x] `npm run build` ينجح.

---

## 1) الدخول والمصادقة
- [x] تسجيل الدخول باسم المستخدم/البريد + كلمة المرور ينجح.
- [x] كلمة مرور خاطئة → رسالة خطأ واضحة.
- [ ] «نسيت كلمة المرور» / إعادة التعيين تعمل (أو «نسخ رابط إعادة التعيين» 👑 من صفحة المستخدمين). — ⏳ يتعذّر محلياً: لا `RESEND_API_KEY` و`NEXT_PUBLIC_SITE_URL=localhost:3000`؛ يُختبر على الإنتاج.
- [x] الخروج يُنهي الجلسة ويعيد لصفحة الدخول.
- [x] محاولة فتح صفحة داخلية بلا تسجيل دخول → تحويل لصفحة الدخول.

---

## 2) إنشاء الخطة (الطريقتان + الافتراضات الجديدة) 👑
- [x] «خطة جديدة» → الخطوة 1: الاسم + العام + **تاريخا البدء/الانتهاء إلزاميان** (الحفظ يُرفض بدونهما).
- [x] الخطوة 2: **الهيكل الرباعي مُحدَّد افتراضياً** بمسمّياته: المعيار الرئيس · الجانب · المعيار الفرعي · الهدف.
- [x] تغيير عدد المستويات (2–5) يحدّث المسمّيات والمعاينة.
- [x] الخطوة 3 (KPI): **المعيار الفرعي مُفعَّل افتراضياً فقط**، والبقية مُعطَّلة.
- [x] يظهر **زرّان**: «📋 إنشاء بطريقة القوائم» (مع شارة AI Powered) و«🌳 إنشاء بطريقة العقد الهرمية».
- [x] زر «القوائم» يُنشئ الخطة ويفتح **صفحة البناء بالقوائم**.
- [x] زر «الهرمية» يُنشئ الخطة ويفتح **صفحة العرض الشجري**.

---

## 3) بناء الخطة — العرض الهرمي (الشجرة) 👑
- [x] إضافة «معيار رئيس» من **كتالوج الاعتماد** أو «بند مخصّص» (نص حر).
- [x] الدخول للمعيار: إضافة جانب → معيار فرعي → هدف (تتابع المستويات).
- [x] المستويات 1–3 تعرض كتالوج الاعتماد؛ الهدف نص حر.
- [x] تعديل اسم عقدة · توسيع/طيّ الشجرة.
- [x] **الترقيم يظهر** بجانب كل عنصر (رسمي للمعايير، هرمي محسوب للأهداف).
- [x] مؤشرات الأداء (KPI) تظهر على المعيار الفرعي (مُفعَّل) — إضافة يدوية + توليد AI.

## 4) بناء الخطة — عرض القوائم المتتالية (جديد) 👑
- [x] فتح قائمة «المعيار الرئيس» تعرض **كتالوج الاعتماد مباشرةً** (بلا نقرة زائدة) + «بند مخصّص».
- [x] اختيار معيار من الكتالوج يضيفه فوراً ويُحدّده وينتقل للمستوى التالي.
- [x] «مخصّص» يفتح حقل نص حر ويُضيف بالاسم المكتوب.
- [x] التتابع يعمل حتى الهدف، ثم يظهر قسم المهام.
- [x] **اقتراح أهداف بالذكاء الاصطناعي** (عند المعيار الفرعي): توليد 6 → اختيار → «إضافة المحدد» تُضيفها مرقّمة.
- [x] **اقتراح مهام بالذكاء الاصطناعي** (عند الهدف): توليد → اختيار → إضافة.
- [x] **الإدخال السريع للمهام:** كتابة اسم + Enter يُضيف مهمة فوراً ويُفرّغ الحقل (إضافة عدة مهام متتالية).
- [x] «فتح النموذج الكامل» ينقل لصفحة إنشاء المهمة بالتفاصيل.
- [x] المحتوى **موسَّط** أسفل رأس الخطة، واسم الخطة على **سطر واحد**.

## 5) الترقيم الهرمي + الحذف المتسلسل (جديد) 👑
- [x] الهدف الجديد يأخذ رقماً هرمياً تلقائياً (كود الأب + الترتيب)، والمهمة كذلك.
- [x] **حذف مهمة** → يُعاد ترقيم باقي مهام الهدف تلقائياً بلا فجوات.
- [x] **حذف هدف** → نافذة تأكيد تذكر **عدد المهام التي ستُحذف معه**.
- [x] بعد حذف الهدف → تُعاد ترقيم الأهداف الباقية تلقائياً (التالي يأخذ رقم المحذوف).
- [x] حذف معيار/جانب → يحذف كل ما تحته (عقد + مهام) — تحقّق أنها اختفت فعلاً.

## 6) التكافؤ بين العرضين + التحويل (جديد) 👑
- [x] صفحة الخطط: تحت كل خطة زرّان «🌳 الهيكل الهرمي» و«📋 القوائم» بنفس النمط واللون.
- [x] رأس الخطة في **عرض القوائم** يطابق العرض الهرمي: نسبة الإنجاز · تقدير الجودة · تصدير/استيراد Excel · إعدادات KPI · لوحة KPI · تقرير QNSA · اعتماد · تعديل · حذف.
- [x] زر التحويل بين العرضين يعمل في الاتجاهين (هرمي ↔ قوائم) لنفس الخطة.
- [x] التعديل/الاستيراد/إعدادات KPI من **داخل عرض القوائم** يعمل ويحدّث الخطة.

---

## 7) سير عمل المهمة (آلة الحالات)
- [x] 👤 «لم تبدأ → جارية» (بدء العمل).
- [x] 👤 رفع دليل ثم «رفع للتقييم».
- [x] 👤 **لا يستطيع** اعتماد/رفض الدليل الذي رفعه (الأزرار مخفية + الخادم يرفض) — منع التقييم الذاتي.
- [x] 🧑‍⚖️ اعتماد/رفض الدليل + سبب الرفض.
- [x] 🧑‍⚖️ اعتماد المهمة + تقييم (1–5) → «منجزة».
- [x] 🧑‍⚖️ «إعادة للتعديل» + سبب → ترجع «جارية» + إشعار للمكلَّف.
- [x] منع التقييم الذاتي للمهمة (المقيّم ≠ المكلَّف).
- [x] 👑 «إعادة فتح» مهمة منجزة → تُصفَّر دورة التقييم.
- [x] كل انتقال يُسجَّل في «سجل سير العمل» وينعكس في تقرير QNSA.
- [x] المهمة المنجزة مقفلة من التعديل (عدا التعليقات) + لافتة القفل.

## 8) بوّابة الإنجاز + أنواع الأدلة
- [x] عند إنشاء/تعديل المهمة: تحديد «أنواع الأدلة المطلوبة».
- [x] صفحة المهمة تُظهر بطاقة البوّابة (مقبول/قيد المراجعة/ناقص) + شارة الاكتمال.
- [x] لا يُعتمد قبل قبول دليل لكل نوع مطلوب.
- [x] لا يُعتمد إن كانت كل الأدلة مرفوضة.
- [x] أدلة متنوعة: ملفات متعددة · رابط فيديو يوتيوب (صورة مصغّرة + QR) · وصف AI.
- [x] تعديل الدليل (تسمية/إضافة/حذف مرفقات) + صفحة الغلاف/الطباعة الذكية.

## 8ب) خزانة الأدلة (العدسة الإشرافية)
- [x] بطاقات الإحصاء (العدد · التغطية% · المعتمدة · المشتركة · الحجم) تعرض أرقاماً صحيحة.
- [x] تبويب «الأدلة» يسرد كل أدلة المدرسة مع المعيار · القسم · النوع · الحالة.
- [x] تبويب «التغطية» يُظهر تحليل تغطية المعايير (المغطّى مقابل الناقص).
- [x] الفلاتر (المعيار الرئيس · الفرعي · الهدف · القسم · النوع · الحالة) ترشّح القائمة وعدّاد النتائج يتحدّث.
- [x] فلتر «المشتركة فقط» يعرض الأدلة المرتبطة بأكثر من معيار.
- [x] البحث بالاسم أو الرقم يرشّح القائمة فورياً.
- [x] شارة حالة كل دليل (معتمد/مرفوض/قيد المراجعة) تطابق حالته الفعلية.
- [x] النقر على دليل يفتحه/يُنزّله.
- [x] العزل/التقمّص 👑: تظهر أدلة المدرسة الفعّالة فقط بلا تسرّب.

## 9) التقويم المدرسي + التواريخ
- [x] تاريخ البدء (افتراضي اليوم) + الانتهاء إلزاميان.
- [x] تاريخ ضمن عطلة «منع» → يُرفض؛ 👑 يتجاوز بتأكيد.
- [x] تاريخ ضمن اختبارات/نهاية أسبوع → تنبيه يسمح بالمتابعة.
- [x] عدسة التقويم تُظلّل: عطلات (أحمر) · اختبارات (أصفر) · نهاية الأسبوع (رمادي).
- [x] 👑 الإعدادات ← التقويم المدرسي: إضافة/تعديل/حذف فترة + ضبط نهاية الأسبوع.

## 10) العدسات الثلاث
- [x] **مهامي:** التبويبات + بحث + فرز + «المتأخرة فقط» + نقطة الأولوية.
- [x] **كل المهام:** بطاقات الحالة في صف واحد + الفلاتر + «إزالة كل الفلاتر».
- [x] فلتر التقييم: «لم تُقيَّم» يُعطّل صفّ الدرجات.
- [x] شارة/فلتر القسم + ترقيم الصفحات (السابق/التالي).
- [x] **كانبان:** السحب «لم تبدأ→جارية» يعمل؛ «→منجزة» يفتح المهمة للتقييم؛ نقلة ممنوعة → رسالة.
- [x] **جانت/التقويم:** يعرضان المُصفّى؛ المهام بلا تواريخ لا تظهر في جانت.
- [x] رقم المهمة الهرمي يظهر في القوائم (جديد).

## 11) لوحة التجميع + الاتجاه 🧑‍⚖️/👑
- [x] تثبيت الأعلى + الفلاتر المضغوطة.
- [x] التجميع حسب القسم/النوع/المالك.
- [x] «أدلة مقبولة» تَعدّ المقبول فقط.
- [x] روابط الغوص تفتح «كل المهام» مُفلتراً.
- [x] تصدير Excel (xlsx) يفتح عربياً منسّقاً مع ترويسة وعرض أعمدة.
- [x] قسم «اتجاه نسبة الإنجاز» يرسم الخط (لقطات pg_cron).
- [x] العزل: مشرف=الكل · نائب=أقسامه · منسق=قسمه.

## 12) مركز التقارير الرسمية + QNSA 👑
- [x] الإعدادات ← بيانات المدرسة: رفع شعار + توقيع المدير + الختم.
- [x] المُشغّل: اختيار تقرير + (للزمنية) منتقي الفترة + «إصدار».
- [x] كل تقرير يطبع/يُحفظ PDF بالترويسة والتوقيع والختم.
- [x] تقرير QNSA يُظهر «سجل الاعتماد والمراجعة».
- [x] التقارير الزمنية (تدقيق/مُعادة/تقدير/اتجاه) تحترم الفترة.

## 13) نوافذ تأكيد الحذف الموحّدة (جديد)
- [x] كل حذف في التطبيق يفتح **نافذة منبثقة موحّدة** (لا نص صغير مضمَّن): الخطط · العقد · المهام · الأدلة · المستخدمون · الفرق · الأوسمة · المدارس · الاجتماعات · الإعدادات (دور/خيار) · الأماكن · التقويم · المجموعات.
- [x] النافذة تُغلق بـ Esc أو النقر خارجها، وتعرض اسم العنصر (وعدد المهام التابعة عند العقد).

## 14) الإعدادات (التنظيم الجديد) 👑
- [x] **بيانات المدرسة هي الصفحة الافتراضية** عند فتح الإعدادات.
- [x] الشريط الجانبي: مجموعتان قابلتان للطيّ (البنية التنظيمية · القوائم والتصنيفات) بعدّادات + عناصر مباشرة.
- [x] **مربع البحث أعلى الصفحة** يرشّح كل عناصر الإعدادات (نتائج مسطّحة).
- [x] فتح مجموعة يطوي الأخرى؛ آخر قسم مفتوح يُحفظ ويُستعاد.
- [x] إدارة كل قسم تعمل: بيانات المدرسة · الأماكن · إشراف الأقسام · أعضاء الأقسام · التقويم · القوائم الستّ · الأدوار · إرسال إشعار.

## 15) المستخدمون + الأدوار 👑
- [x] إنشاء مستخدم + تعيين دور + عضوية فرق (تُحفظ بلا خطأ).
- [x] تعديل اسم المستخدم يحدّث الاسم في كل الواجهة (لا اسمان مختلفان).
- [x] حذف مستخدم عبر النافذة الموحّدة؛ **منع حذف الحسابات المميّزة/الذات** (رسالة واضحة).
- [x] الأدوار: إنشاء/تعديل دور + ضبط صلاحياته؛ منع تصعيد الصلاحيات من غير المخوّل.

## 16) العزل وتعدد المدارس والتقمّص (أمني — مهم) 👑
- [x] بلا `view_aggregate` → لوحة التجميع محجوبة. بلا `manage_plans` → الخطط محجوبة. بلا `view_reports` → التقارير محجوبة.
- [x] **التقمّص:** مشرف نظام يدخل مدرسة → كل البيانات تخصّ المدرسة الفعّالة فقط (لا تسرّب عبر المدارس).
- [x] إنشاء/حذف خطة أو مستخدم أثناء التقمّص يُسند للمدرسة الفعّالة الصحيحة.
- [x] محاولة استدعاء API لمهمة/دليل/خطة خارج المدرسة → 403.

## 17) الموبايل والمتصفحات والطباعة
- [x] شاشة ~375px: لا فيض أفقي، الأزرار قابلة للنقر، الجداول قابلة للتمرير، الشريط الجانبي للإعدادات مناسب.
- [ ] Chrome · Edge · Safari: العرض والطباعة سليمة. — ⏳ Chrome/Edge ✅ محلياً · Safari بانتظار جهاز Apple.
- [x] RTL سليم في كل الصفحات (لا تكسّر اتجاه).
- [x] طباعة الأدلة/التقارير: ترويسة وتوقيع وختم سليمة، بلا واجهة التطبيق.

---

## 18) عمليات قبل الإطلاق (إعداد/بيانات — لا اختبار وظيفي)
- [ ] **تمريرة الإنتاج بعد النشر:** الدخول · إعادة تعيين كلمة مرور مستخدم · **نسخ كلمة المرور المؤقتة** (موضع الانهيار المُصلَح) · طباعة تقرير.
- [ ] **بريد:** تأكيد عمل إشعارات Resend + مسار إعادة تعيين كلمة المرور (أو الاكتفاء بـ«نسخ الرابط»).
- [ ] **التقويم:** إضافة عطلتي العيدين فور إعلان الوزارة (الإعدادات ← التقويم).
- [ ] **(مؤجَّل لخطة Pro):** تفعيل «حماية كلمات المرور المسرّبة» — غير مانع للإطلاق.
- [x] **اختياري:** رفع حد طول كلمة المرور إلى 8 + متطلباتها (Auth → Email — مجاني). ✅ 2026-07-02: `Minimum password length=8` + `Password requirements=Letters and digits` — يطابق تماماً تحقّق الكود الموجود مسبقاً في `update-password/page.tsx` وغيره.

---

## سجل المشاكل المكتشفة
| # | الصفحة/الميزة | الوصف | الدور | الحالة |
|---|----------------|-------|-------|--------|
| 1 | المستخدمون — زرّ «نسخ كلمة المرور المؤقتة» | انهيار الصفحة بـ `insertBefore` عند النقر (أيقونة شرطية بجوار نص شرطي في عنصر واحد) — تكرّر على الإنتاج وفي نافذة متخفية | 👑 | ✅ أُصلح 2026-09-09 + عُولج النمط نفسه في 18 موضعاً عبر التطبيق |
| 2 | الخطط — فتح `/dashboard/plans` مباشرةً بلا صلاحية | حارس الصلاحية كان يعود قبل الـhooks ⇒ «Rendered fewer hooks than expected» بدل رسالة «غير متاح» | 👤 | ✅ أُصلح 2026-09-09 (ESLint rules-of-hooks 12→0) |
| 3 | رسائل الأخطاء | 23 رسالة بنوافذ `alert()` أصلية بدل `toast` الموحّد | الكل | ✅ وُحِّدت 2026-09-09 |

---

> **معيار الجاهزية للإطلاق:** اجتياز كل بنود الأقسام 1–17 + إنهاء عمليات القسم 18 (عدا المؤجَّل لـPro). أي ❌ يُسجَّل ويُعالَج قبل الإعلان.
