# خطة العمل (المرحلة الثانية) — سير عمل المهام والأدلة + الإطلاق

**تاريخ البدء:** 2026-06-09 | **المدة:** ~3 أسابيع (12 يوم عمل)
**تَسبق:** هذه الخطة تُكمل ما تبقّى من `WORKPLAN.md` (الاختبار النهائي + الإطلاق رُحّلا لنهايتها).

> ### الاستخدام اليومي
> اكتب: **"اقرأ PROJECT_CONTEXT.md و WORKPLAN_V2.md ثم نفّذ [الأسبوع X - اليوم Y]"**
> بعد إنجاز كل يوم: ✅ + التاريخ الفعلي.

---

## 🐞 أخطاء معلّقة من الاختبار (أولوية عالية — تُعالَج قبل إكمال الأسبوع 2)

> رُصدت أثناء اختبار مشرف نظام متقمّص "مدرسة أحمد منصور الابتدائية" (2026-06-10).

### 1) إدارة المستخدمين — حذف الحسابات المميّزة بلا حماية 🔴
- `users/page.tsx` (~سطر 411): الحذف يتم مباشرةً `supabase.from('profiles').delete().eq('id', id)` ثم إزالة **تفاؤلية** من الواجهة.
- **المشكلة:** يسمح بحذف `super_admin` (حازم خلف) و`school_admin` (أحمد التجريبي) والذات، بلا تحذير.
- **الإصلاح المطلوب:** نقطة API خادمية للحذف:
  - تمنع حذف `is_super_admin` و`school_admin`/`admin` والمستخدم نفسه (رسالة واضحة).
  - تتحقق من النتيجة فعلياً (لا إزالة تفاؤلية إلا بعد نجاح القاعدة).
  - يُفضّل soft-delete أو تنظيف auth.users المرافق (حالياً hard delete قد يُيتّم auth.users).

### 2) عودة المحذوفين بعد تحديث الصفحة 🔴
- السبب: الواجهة تزيل الصف "تفاؤلياً" حتى لو منعت RLS الحذف فعلياً (الحسابات المميّزة لم تُحذف من القاعدة) → تظهر مجدداً عند `loadAll()`.
- **الإصلاح:** بعد نجاح الـ API فقط تُزال من الواجهة.

### 3) المستخدم الجديد لا يظهر أثناء التقمّص 🔴
- `/api/users/create`: لا يحترم التقمّص — يُسند المستخدم الجديد لمدرسة المشرف لا للمدرسة المتقمّصة (`active_school_id`).
- **الإصلاح:** يحسب المدرسة الفعّالة (نمط `getContext`: `is_super_admin && active_school_id ? active_school_id : school_id`) ويُسندها للمستخدم الجديد.

### 4) نمط خطر عام: `schools.select('id').single()` 🟠
- أُصلح في إنشاء الخطة (عبر `/api/plans`). **يجب تفحّص بقية الصفحات** التي تحدّد المدرسة بهذه الطريقة (تفشل لمشرف النظام الذي يرى عدة مدارس → 406، أو تُسند المدرسة الخطأ). البديل: API يحسب المدرسة الفعّالة.

---

## 💡 دروس مستفادة (مهمة — لا تُنسَ)
1. **Next.js معدّلة (Turbopack):** اقرأ `node_modules/next/dist/docs/` قبل أي كود (تحذير AGENTS.md).
2. **خطأ `insertBefore`/`removeChild` في React:** سببه نمط **أيقونة شرطية بجوار نص شرطي** داخل عنصر — اعزل الأيقونة في `<span className="inline-flex">` ثابت. (تكرّر مرتين: رفع الشعار، حفظ الدور).
3. **خطأ `X is not a function` رغم استيراد X:** تحقّق من **متغير محلي يحجب المستورد** (shadowing) — حدث مع `isOverdue` (متغير boolean محلي حجب الدالة المستوردة). ابحث عن تعريف محلي بنفس الاسم أولاً.
4. **التشخيص الصحيح للأخطاء المُصغّرة:** شغّل نسخة التطوير محلياً (`npm run dev`) لرؤية الخطأ غير المُصغّر بالسطر والمكوّن — أسرع من التخمين.
5. **عند إضافة قيم حالة/enum جديدة:** تحقّق من **قيود CHECK في القاعدة** (مثل `tasks_status_check` الذي رفض submitted/returned صامتاً والـ API يبتلع الخطأ).
6. **افحص أخطاء القاعدة:** الـ API لا يجب أن يبتلع خطأ `UPDATE/INSERT` ويُرجع ok — افحص `error` و`.select()` لعدد الصفوف.
7. **تعدد المدارس + التقمّص:** أي عملية تُحدّد المدرسة يجب أن تستخدم **المدرسة الفعّالة** (`active_school_id` عند تقمّص المشرف) عبر API خادمي، لا `schools.single()` من المتصفح.
8. **الحراسة طبقتان:** واجهة (إخفاء) + خادم (رفض) + عزل المدرسة — لا تكتفِ بالواجهة.
9. **التصدير من الوحدات المشتركة:** استخدم `export const` (لا تشغل بالنا بـ function؛ المشكلة الحقيقية كانت الـ shadowing لا نوع التصدير).

---

## 🎯 الهدف
تحويل المهمة من "حالة يحدّدها المكلّف يدوياً" إلى **سير عمل مُقنَّن قائم على الأدلة والمراجعة**، يعطي مؤشراً حقيقياً على الإنجاز ويقوّي مصداقية تقرير QNSA (سجل موثّق: من رفع/اعتمد/قيّم ومتى).

## 🧩 النموذج المعتمد (مرجع التصميم)

**آلة حالات المهمة:**
```
لم تبدأ ──[المكلّف]──> جارية ──[المكلّف]──> مرفوعة للتقييم
                                              │ [المقيّم]
                          ┌───────────────────┴───────────────────┐
              معتمدة → منجزة + تقييم جودة (1-5)        مُعادة للتعديل (+سبب) → ترجع "جارية"
```
- **المكلّف يملك:** لم تبدأ · جارية · **مرفوعة للتقييم** (آخر ما يملكه).
- **المقيّم يملك:** اعتماد (→ منجزة + تقييم) · إعادة (+ سبب). يُمنع أن يكون المقيّم هو المكلّف.
- **متأخرة:** وسم **محسوب** (موعد فات وغير منجزة)، يُعرض فوق أي حالة — لا حالة مستقلة.
- **دورة الدليل:** مرفوع → (المقيّم) معتمد/مرفوض + سبب.
- **أنواع الأدلة المطلوبة:** اختيارية لكل مهمة (مصدرها القوائم المنسدلة). إن حُدّدت → لا إنجاز قبل اعتماد دليل لكل نوع مطلوب؛ وإلا فاعتماد المقيّم كافٍ.
- **شرط الإنجاز:** رفع المكلّف + (اعتماد الأدلة المطلوبة إن وُجدت) + اعتماد المقيّم + تقييم الجودة.

## 🗄️ تغييرات قاعدة البيانات (تُنفّذ في اليوم 1)
- `tasks.status`: قيم جديدة `submitted` (مرفوعة) و`returned` (مُعادة)؛ إيقاف استخدام `delayed` كحالة (يُحسب كوسم).
- `tasks`: + `submitted_at`, `submitted_by`, `return_note`, `required_evidence_types TEXT[]`.
- `evidence`: + `status` (pending/accepted/rejected) + `review_note` + `evidence_type` + `reviewed_by` + `reviewed_at`.
- جدول جديد `task_transitions` (سجل التحوّلات: task_id, from, to, actor_id, note, at) — مع RLS بالمدرسة.
- `dropdown_options`: فئة جديدة `evidence_type` (بذور: خطة · محضر اجتماع · تقرير · صور · مستند · نتائج).
- ترحيل البيانات: `delayed` الحالية → `in_progress`؛ الأدلة الحالية → `accepted` (لئلا تتعطّل المهام المنجزة).

---
---

## 🟦 الأسبوع الأول — قلب سير العمل (Phase 1)

### اليوم 1 | التصميم + ترحيل قاعدة البيانات ✅ 2026-06-09
- [x] مستند تصميم `docs/TASK_WORKFLOW.md` (آلة الحالات + الصلاحيات + الحالات الحدّية)
- [x] `migrations/021_task_workflow.sql` (الحقول + جدول التحوّلات + فئة evidence_type + ترحيل البيانات)
- [x] `STATUS_META`: إضافة submitted/returned + `OVERDUE_META` + `isOverdue()` في `lib/constants/tasks.ts`
- [ ] **يدوي:** تشغيل `migrations/021` في Supabase

### اليوم 2 | منطق الانتقالات + الصلاحيات (الخادم) ✅ 2026-06-09
- [x] `api/tasks/[taskId]/transition` (submit / approve / return):
  - عزل بالمدرسة (node→plan→school) + تحديد المكلّف (مباشر أو عبر الفريق)
  - المكلّف يرفع؛ المقيّم/صاحب rate_tasks يعتمد/يعيد؛ **منع التقييم الذاتي**
  - بوابات: الأدلة المطلوبة مرفوعة عند الرفع، ومعتمدة عند الاعتماد
  - تسجيل كل انتقال في `task_transitions` + إشعار الطرف الآخر
  - المقيّم الفعّال: reviewer_id أو fallback لصاحب rate_tasks/all

### اليوم 3 | شاشات المكلّف والمقيّم (صفحة المهمة) ✅ 2026-06-09
- [x] إضافة إجراء `start` للـ API (بدء العمل)
- [x] لوحة المكلّف: "بدء العمل" + "رفع للتقييم" (بدل تحديد "منجزة" يدوياً)
- [x] لوحة المقيّم: اعتماد (+تقييم 1-5) / إعادة (+سبب) — مرتبطة بالحالة `submitted`
- [x] عرض الحالة + وسم التأخير المحسوب + سبب الإعادة
- [ ] (يتبقّى Day 5) منع التعديل الحرّ للحالة في List/Kanban/my-tasks

### اليوم 4 | الإشعارات + سجل التحوّلات ✅ 2026-06-10
- [x] الإشعارات (نُفّذت في اليوم 2: رفع→المقيّم، اعتماد/إعادة→المكلّف)
- [x] عرض **سجل التحوّلات** في صفحة المهمة (الإجراء + المنفّذ + التاريخ + السبب) — يحفظ كل دورة
- [x] إظهار **سبب الإعادة** (إضافة return_note لجلب الصفحة)
- [x] **منع المكلّف من حذف المهمة** (الحذف للمدير أو منشئ المهمة فقط)

### اليوم 5 | "متأخرة" وسماً + تحديث العروض ✅ 2026-06-10
- [x] إزالة "delayed" كحالة من STATUS_META (صارت وسماً محسوباً via isOverdue/OVERDUE_META)
- [x] `cron/update-delayed`: توقّف عن الكتابة فوق status — يُشعر فقط (فلتر ≠ completed)
- [x] "كل المهام": الحالة **للعرض فقط** (أُزيلت القائمة المنسدلة الحرّة)
- [x] "مهامي": أزرار الحالة الحرّة → رابط "فتح المهمة لإدارة الحالة" + إصلاح خطأ team_members (user_id→profile_id)
- [x] Kanban: أعمدة جديدة (مرفوعة/مُعادة بدل متأخرة) + تعطيل تغيير الحالة بالسحب (سير العمل من صفحة المهمة)

---
---

## 🟩 الأسبوع الثاني — الأدلة (Phase 2 + 3)

### اليوم 6 | تحقّق الدليل (اعتماد/رفض)
- [ ] ضمن لوحة المقيّم: اعتماد/رفض كل دليل + سبب الرفض
- [ ] عرض حالة كل دليل (مرفوع/معتمد/مرفوض) للمكلّف

### اليوم 7 | أنواع الأدلة + التصنيف عند الرفع
- [ ] فئة `evidence_type` في الإعدادات (القوائم المنسدلة) + بذورها
- [ ] حقل "أنواع الأدلة المطلوبة" عند إنشاء/تعديل المهمة (اختياري)
- [ ] عند رفع الدليل: اختيار نوعه

### اليوم 8 | بوابة الإنجاز
- [ ] منع الاعتماد/الإنجاز قبل توفّر دليل معتمد لكل نوع مطلوب
- [ ] مؤشر بصري في المهمة: الأدلة المطلوبة وحالة كلٍّ منها

### اليوم 9 | صفحة إدارة الأدلة
- [ ] `/dashboard/evidence`: قائمة موحّدة بكل الأدلة (فلترة بالحالة/النوع/المهمة) + تنزيل + مراجعة سريعة

### اليوم 10 | التقارير + تقرير QNSA
- [ ] تحديث التقارير لتعكس الحالات الجديدة (مرفوعة/مُعادة/منجزة)
- [ ] تقرير QNSA: إظهار سجل الاعتماد (من اعتمد/قيّم ومتى) لتعزيز المصداقية
- [ ] Build + Push

---
---

## 🟢 الأسبوع الثالث — الإطلاق (مُرحّل من الخطة السابقة)

### اليوم 11 | اختبار شامل نهائي (كان يوم 59)
- [ ] اختبار التدفق الكامل بأدوار مختلفة (مكلّف/مقيّم/مدير)
- [ ] اختبار الحالات الحدّية (بلا مقيّم، بلا أدلة، إعادة متكررة)
- [ ] اختبار الموبايل + المتصفحات
- [ ] إصلاح ما يظهر

### اليوم 12 | التوثيق + الإطلاق (كان يوم 60)
- [ ] تحديث `PROJECT_CONTEXT.md` بالنموذج الجديد
- [ ] تحديث التوثيق + تواريخ الإنجاز
- [ ] Build نهائي + Push + التحقق من Vercel
- [ ] 🎉 الإطلاق

---

## سجل الإنجازات (المرحلة الثانية)
| التاريخ | اليوم | المنجز | الملفات |
|---------|-------|--------|---------|
| 2026-06-10 | أمان | حصر التكليف/التعديل/الحذف بـ manage_tasks (واجهة) + **حارس خادمي** عبر `/api/tasks/[taskId]` (PATCH/DELETE) | `api/tasks/[taskId]/route`, taskId/page |
| 2026-06-10 | إصلاح | إنشاء الخطة عبر `/api/plans` يحسب المدرسة الفعّالة (إصلاح خطأ RLS لمشرف النظام/التقمّص) | `api/plans/route`, plans/new/page |
| | | | |
