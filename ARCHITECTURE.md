# 🏛️ المعمارية الهرمية — نظام متابعة الخطط المدرسية
**إصدار:** 1.0 | **التاريخ:** 2026-06-07
**الغرض:** توضيح هرمية الأدوار والعزل وأنماط الوصول متعدد المدارس

---

## 1️⃣ الهرمية الكاملة (Tenancy Hierarchy)

```
                    ╔════════════════════════════════╗
                    ║      مستوى المنصة (Platform)    ║
                    ║   مشرف النظام / Super Admin     ║
                    ║   مُشغّل المنصة — بلا مدرسة      ║
                    ╚═══════════════╤════════════════╝
                                    │
                 يدير الكل + يرى أرقاماً مُجمَّعة فقط
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        ▼                           ▼                           ▼
 ╔═════════════════╗        ╔═════════════════╗        ╔═══════════════╗
 ║  مجموعة مدارس أ  ║        ║  مجموعة مدارس ب  ║        ║  مدرسة مستقلة  ║
 ║  (مالك مجموعة)   ║        ║  (مالك مجموعة)   ║        ║  (بلا مجموعة)  ║
 ║  يرى أرقام       ║        ║  يرى أرقام       ║        ╚═══════╤═══════╝
 ║  مدارسه فقط      ║        ║  مدارسه فقط      ║                │
 ╚════════╤════════╝        ╚════════╤════════╝                │
          │                          │                         │
    ┌─────┴─────┐              ┌─────┴─────┐                   │
    ▼           ▼              ▼           ▼                   ▼
 ┌──────┐   ┌──────┐       ┌──────┐   ┌──────┐            ┌──────┐
 │مدرسة1│   │مدرسة2│       │مدرسة3│   │مدرسة4│            │مدرسة5│
 └──┬───┘   └──┬───┘       └──┬───┘   └──┬───┘            └──┬───┘
    │          │              │          │                   │
    └──────────┴───── كل مدرسة Tenant معزول تماماً ─────────┘
                         (لا تَرى الأخرى أبداً)
```

> **ملاحظة:** "مجموعة المدارس" (المرحلة 2) طبقة اختيارية. لو لم تُستخدم،
> فالمشرف يرى كل المدارس مباشرةً كما في المرحلة 1.

---

## 2️⃣ بنية المدرسة الواحدة (Inside a Tenant)

```
 ┌─────────────────────────────────────────────────┐
 │              مدرسة (Tenant معزول)                │
 │                                                  │
 │   الأدوار:                                       │
 │   ├── مدير المدرسة (school_admin) ← صلاحيات كاملة │
 │   ├── منسق الخطة / قائد فريق                      │
 │   └── معلم / منفّذ مهمة                           │
 │                                                  │
 │   البيانات (معزولة بـ RLS عبر school_id):         │
 │   ┌──────────────────────────────────────────┐  │
 │   │  خطة → محاور → مبادرات → أهداف → مهام      │  │
 │   │                          └── أدلة          │  │
 │   │                          └── خطوات فرعية   │  │
 │   │  فرق · اجتماعات · مؤشرات أداء (KPIs)        │  │
 │   │  إشعارات · تعليقات · سجل نشاط · تقارير      │  │
 │   └──────────────────────────────────────────┘  │
 └─────────────────────────────────────────────────┘
```

---

## 3️⃣ أنماط الوصول — جوهر الأمان (Access Patterns)

```
 الدور             │ ما يراه                      │ الحاجز الأمني
 ──────────────────┼──────────────────────────────┼─────────────────────
 مشرف النظام       │ أرقام مُجمَّعة لكل المدارس     │ ❌ لا بيانات فردية
 (Platform)        │ (إنجاز% · متأخرات · عدد خطط)  │ ✅ Service Role، أرقام فقط
 ──────────────────┼──────────────────────────────┼─────────────────────
 مالك المجموعة     │ أرقام مُجمَّعة لمدارس مجموعته  │ ❌ لا بيانات فردية
 (Group) [مرحلة2]  │ + مقارنة بينها                │ ✅ مجموعته فقط
 ──────────────────┼──────────────────────────────┼─────────────────────
 مدير المدرسة      │ كل تفاصيل مدرسته              │ ✅ RLS: مدرسته فقط
 (school_admin)    │ (مهام · مستخدمون · تقارير)    │
 ──────────────────┼──────────────────────────────┼─────────────────────
 منسق / قائد فريق  │ خطط ومهام نطاقه               │ ✅ RLS + صلاحيات الدور
 ──────────────────┼──────────────────────────────┼─────────────────────
 معلم / منفّذ      │ مهامه + ما كُلّف به           │ ✅ RLS + صلاحيات الدور
 ──────────────────┴──────────────────────────────┴─────────────────────
```

---

## 4️⃣ حدود حماية البيانات (The Privacy Boundary)

```
        ╔═══════════════ الخادم (Supabase + API) ═══════════════╗
        ║                                                       ║
        ║   البيانات الفردية الخام                               ║
        ║   (أسماء مهام · مستخدمون · أدلة · تفاصيل)             ║
        ║         │                                             ║
        ║         │  ◄── RLS يعزل كل مدرسة                       ║
        ║         │                                             ║
        ║   ┌─────┴──────┐                                      ║
        ║   │  تجميع      │  ◄── حساب الأرقام فقط                ║
        ║   │ (Aggregate) │     (COUNT · % · متوسطات)           ║
        ║   └─────┬──────┘                                      ║
        ║         │                                             ║
        ╚═════════│═════════════════════════════════════════════╝
                  │
         ┌────────┴─────────┐   ◄══ الحاجز: أرقام فقط تعبُر
         ▼                  ▼
   ┌──────────┐      ┌──────────────┐
   │  المشرف   │      │ مدير المدرسة  │
   │ أرقام     │      │ بيانات مدرسته │
   │ مُجمَّعة   │      │ الكاملة (RLS) │
   └──────────┘      └──────────────┘

  ⚖️  متوافق مع: PDPL القطري · GDPR · FERPA
      (التقارير المُجمَّعة ≠ الوصول للبيانات الفردية)
```

---

## 5️⃣ مراحل التنفيذ

```
 المرحلة 1 (جاهزة للبناء) ─── لوحة المقارنة الإجمالية للمشرف
   └── أرقام كل المدارس + رسم بياني مقارن
   └── Service Role، لا بيانات فردية
   └── لا تعديل RLS، لا فك ارتباط

 المرحلة 2 (عند الحاجة) ──── مفهوم "مجموعة المدارس"
   └── جدول school_groups + schools.group_id
   └── مالك المجموعة يرى أرقام مجموعته فقط

 المرحلة 3 (اختياري) ─────── "الدخول كمدرسة" (View-as)
   └── للدعم الفني / الاختبار العميق
   └── + تسجيل الوصول في audit_logs
```

---

## 6️⃣ مبادئ معمارية ثابتة

| المبدأ | التطبيق |
|--------|---------|
| **العزل أولاً** | كل مدرسة Tenant معزول بـ RLS عبر `school_id` |
| **الأرقام تعبُر، البيانات لا** | المستويات العليا ترى تجميعاً فقط |
| **التدقيق إجباري** | أي وصول للمستوى الأعلى يُسجَّل في `audit_logs` |
| **الصلاحية تُضاف لا تُفترض** | كل دور يبدأ بلا صلاحيات ثم تُمنح صراحةً |
| **الامتثال بالتصميم** | يحترم PDPL/GDPR منذ المعمار لا كإضافة |

---

## 7️⃣ تفصيل المرحلة 3 — "الدخول كمدرسة" (Impersonation)

### النهج الصحيح: تقمّص مؤقت لا حساب منفصل

```
 ❌ الخطأ الشائع           │  ✅ النهج الصحيح
 ─────────────────────────┼──────────────────────────────
 إنشاء حساب مدير منفصل     │  تقمّص مؤقت على حساب المشرف نفسه
 لكل مدرسة                 │  (كما في Stripe / Salesforce)
 ─────────────────────────┼──────────────────────────────
 • كلمة مرور مشتركة        │  • نفس هوية المشرف، نطاق رؤية مؤقت
 • صعب الإلغاء             │  • قابل للتراجع بنقرة "خروج"
 • غموض المسؤولية          │  • مساءلة كاملة في السجل
```

### آلية العمل

```
1. المشرف في لوحة المدارس
        ▼ يضغط "دخول كـ [مدرسة X]"
2. النظام يضبط active_school_id على ملف المشرف
        ▼ my_school_id() = COALESCE(active_school_id, school_id)
3. كل استعلامات المشرف تُفلتر بمدرسة X (نفس RLS الموجود)
        ▼ شريط علوي ثابت: "⚠️ تتصفح كـ [مدرسة X] — [خروج]"
4. "خروج" → active_school_id = NULL → عودة لوضع المشرف
```

### نمطان للوصول

| النمط | الصلاحية | الاستخدام | الافتراضي |
|-------|----------|-----------|-----------|
| **عرض كـ** (View-as) | قراءة فقط | الدعم الفني | ✅ |
| **تصرّف كـ** (Act-as) | قراءة + تعديل | الاختبار | يُفعَّل صراحةً |

### التدقيق الإجباري (audit_logs)

```
🕐 دخل المشرف [حازم] كـ "مدرسة X"           — 10:23
🕐 خرج المشرف [حازم] من "مدرسة X"          — 10:31 (8 دقائق)
🕐 [تعديل أثناء تقمّص حازم] غُيّرت حالة مهمة  — (في وضع Act-as فقط)
```

### المتطلبات التقنية

| العنصر | التغيير |
|--------|---------|
| `profiles.active_school_id` | عمود جديد (المدرسة المُتقمَّصة حالياً) |
| `my_school_id()` | `COALESCE(active_school_id, school_id)` |
| شريط التقمّص | مكوّن علوي دائم أثناء الجلسة |
| تسجيل | enter / exit / كل تعديل في audit_logs |
| اختياري | إشعار مدير المدرسة بالوصول (شفافية GDPR) |
