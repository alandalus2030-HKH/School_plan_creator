# خطة العمل (المرحلة الثانية) — سير عمل المهام والأدلة + الإطلاق

**تاريخ البدء:** 2026-06-09 | **المدة:** ~3 أسابيع (12 يوم عمل)
**تَسبق:** هذه الخطة تُكمل ما تبقّى من `WORKPLAN.md` (الاختبار النهائي + الإطلاق رُحّلا لنهايتها).

> ### الاستخدام اليومي
> اكتب: **"اقرأ PROJECT_CONTEXT.md و WORKPLAN_V2.md ثم نفّذ [الأسبوع X - اليوم Y]"**
> بعد إنجاز كل يوم: ✅ + التاريخ الفعلي.

---

## 🐞 أخطاء معلّقة من الاختبار (أولوية عالية — تُعالَج قبل إكمال الأسبوع 2)

> رُصدت أثناء اختبار مشرف نظام متقمّص "مدرسة أحمد منصور الابتدائية" (2026-06-10).

### 1) إدارة المستخدمين — حذف الحسابات المميّزة بلا حماية ✅ 2026-06-11
- **أُصلح:** نقطة API خادمية جديدة `DELETE /api/users/[userId]`:
  - تمنع حذف `is_super_admin` و`school_admin`/`admin` والذات (403 برسالة واضحة).
  - فحص صلاحية `manage_users` + عزل المدرسة الفعّالة (تحترم التقمّص).
  - حذف نهائي: `auth.admin.deleteUser` (يتسلسل لـ profiles) + تحقق فعلي من زوال الصف.
  - عند قيد FK (مستخدم مرتبط بمهام/أدلة): رسالة 409 تقترح تعطيل الحساب بدلاً من الحذف.
- الواجهة: إخفاء زر الحذف للحسابات المحمية + جلب `is_super_admin`.
- **اكتشاف:** سبب "فشل الحذف الصامت" على الأرجح قيود FK (جداول كثيرة تشير لـ profiles بلا CASCADE) وليس RLS — سياسات profiles كانت `FOR ALL` أي تسمح بالحذف لأي مستخدم في المدرسة! → الترحيل 023.

### 2) عودة المحذوفين بعد تحديث الصفحة ✅ 2026-06-11
- **أُصلح:** `deleteProfile` يستدعي الـ API ولا يزيل الصف إلا بعد نجاح فعلي؛ عرض الخطأ في صف التأكيد. (أُصلح أيضاً فحص الخطأ في `toggleActive`.)

### 3) المستخدم الجديد لا يظهر أثناء التقمّص ✅ 2026-06-11
- **أُصلح:** `/api/users/create` يحسب المدرسة الفعّالة (نمط `getContext`) ويُسندها للمستخدم الجديد + أُضيف فحص `manage_users` (كانت النقطة مفتوحة لأي مستخدم مسجّل!) + أُزيل fallback "أقدم مدرسة" الخطير (400 بدلاً منه).

### 4) نمط خطر عام: `schools.select('id').single()` ✅ 2026-06-11 (فُحص)
- فُحص كامل `src/`: كل الاستخدامات المتبقية آمنة (`.eq('id', …)` محدد أو خادمية عبر `ctx.schoolId`). لا إصلاح مطلوب.

### ⏳ يدوي (مطلوب منك): تشغيل `migrations/023_profiles_no_client_delete.sql`
- يفكّك سياسات profiles من `FOR ALL` إلى SELECT/INSERT/UPDATE بلا DELETE — يسدّ ثغرة حذف أي ملف شخصي من المتصفح. الحذف يصبح حصراً عبر الـ API الخادمي.
- ✅ شُغّل 2026-06-11 وتُحقق منه.

### ترحيل 028 — ✅ شُغّل 2026-06-12، لكن التشخيص النهائي أعمق
- **الحذف الناعم للخطط والعقد كان معطّلاً للجميع منذ الترحيل 016.** التجارب المخبرية أثبتت أن العلة في سياسة **القراءة** (تُفرض على الصف الجديد) لا في سياسة التحديث — فالترحيل 028 (تفكيك السياسات) تحصين جيد لكنه لا يحل الحذف. **الحل المنفَّذ:** API خادميان `DELETE /api/plans/[planId]` و`DELETE /api/plans/[planId]/nodes/[nodeId]` (manage_plans + عزل المدرسة + تحقق فعلي) والواجهة تستدعيهما في 4 مواضع بلا أي إزالة تفاؤلية. (راجع الدرس 13)

### ترحيل 027 (كتالوج معايير QNSA) — ✅ شُغّل وتُحقق منه (5/15/73) 2026-06-12
- **نُفّذت المرحلة 1 كاملة:** StandardPicker (قوائم متسلسلة من الكتالوج عند إنشاء عقد المستويات 1-3 + بند مخصص برقم تلقائي) في النقاط الثلاث، وحفظ/عرض `standard_code`، والترقيم الرسمي للمهام (كود المعيار + ترقيم آلي للأهداف/المهام) مع توافق كامل للخطط القديمة.
- **المرحلة 2 (لاحقاً):** تقرير تغطية المعايير (Gap Analysis) + قسم "معايير QNSA" في الإعدادات لإدارة البنود المخصصة + ربط الاستيراد من Excel بالكتالوج.

### تفاصيل الترحيل (للسجل):
- **كتالوج معايير الاعتماد الرسمي**: جدول `qnsa_standards` + زرع 5 معايير رئيسة و15 جانباً و73 معياراً فرعياً (مستخرجة ومدققة **بصرياً** من الدليل الإرشادي 207 صفحات) + عمود `plan_nodes.standard_code` + RLS (البنود الرسمية مقفلة؛ المخصصة لكل مدرسة خلف manage_plans/manage_settings).
- تصحيحات موثقة عن الملخص القديم: أسماء المعايير الرئيسة الخمسة، معيار 4 "السلوك والهوية الوطنية"، معيار 5 "إدارة الموارد"، جانب 3.1 "التحصيل الأكاديمي للطلبة"، حذف بند 5.1.5 غير الرسمي، تصحيح TIMSS.
- **التالي بعد تشغيله:** القوائم المتسلسلة في إنشاء عقد الخطة + الترقيم الرسمي + قسم الإعدادات (المرحلة 1 من تصميم Framework Alignment المعتمد).

### ترحيل 026 (تحصين RLS) — ✅ شُغّل وتُحقق منه بالكامل 2026-06-11
(لا جدول بلا RLS، وكل سياسات الكتابة مربوطة بصلاحيات الأدوار عبر `my_perm()`)
🔴 كان عاجلاً — تفاصيل الثغرات أدناه للسجل:
مسح شامل لـ pg_policies (2026-06-11) كشف ثغرات حرجة، الترحيل يغلقها كلها:
- 🔴 **roles**: سياسة بشرط `true` — أي مستخدم كان يستطيع رفع صلاحيات دوره إلى `all` من console (تصعيد صلاحيات كامل).
- 🔴 **schools**: كتابة مفتوحة لأي مستخدم مسجّل **بلا عزل** — تعديل/حذف أي مدرسة بالمنصة.
- 🔴 **plan_nodes**: RLS **معطَّل بالكامل** — عقد خطط كل المدارس مكشوفة قراءةً وكتابةً.
- 🟠 dropdown_options (شرط true) / teams / team_members / meetings / meeting_attendees / motivational_quotes / kpi_readings.
- الأساس: دالة `my_perm(p)` جديدة (SECURITY DEFINER) تربط كتابة العميل بصلاحيات الدور من جدول roles.

### ترحيلات قفل المهمة المنجزة — ✅ شُغّلت وتُحقق منها 2026-06-11
- **024**: الطبقة الثالثة للقفل (منع تعديل المهمة/الأدلة/الخطوات الفرعية من المتصفح للمهمة المنجزة + منع تعيين "منجزة" من العميل + إلغاء الحذف الصلب للمهام). التعليقات (`task_comments` بسياسة ALL) مفتوحة **عمداً** — قرار تصميمي.
- **025**: أعمدة طلب إعادة الفتح (`reopen_requested_by/at/note`) — البطاقة المعلّقة للمشرف + "قيد الانتظار" للطالب + العبارة الآلية في سجل سير العمل.

---

## 💡 دروس مستفادة (مهمة — لا تُنسَ)
1. **Next.js معدّلة (Turbopack):** اقرأ `node_modules/next/dist/docs/` قبل أي كود (تحذير AGENTS.md).
2. **خطأ `insertBefore`/`removeChild` في React:** سببه نمط **أيقونة شرطية بجوار نص شرطي** داخل عنصر — اعزل الأيقونة في `<span className="inline-flex">` ثابت. (تكرّر مرتين: رفع الشعار، حفظ الدور).
3. **خطأ `X is not a function` رغم استيراد X:** تحقّق من **متغير محلي يحجب المستورد** (shadowing) — حدث مع `isOverdue` (متغير boolean محلي حجب الدالة المستوردة). ابحث عن تعريف محلي بنفس الاسم أولاً.
4. **التشخيص الصحيح للأخطاء المُصغّرة:** شغّل نسخة التطوير محلياً (`npm run dev`) لرؤية الخطأ غير المُصغّر بالسطر والمكوّن — أسرع من التخمين.
5. **عند إضافة قيم حالة/enum جديدة:** تحقّق من **قيود CHECK في القاعدة** (مثل `tasks_status_check` الذي رفض submitted/returned صامتاً والـ API يبتلع الخطأ).
6. **افحص أخطاء القاعدة:** الـ API لا يجب أن يبتلع خطأ `UPDATE/INSERT` ويُرجع ok — افحص `error` و`.select()` لعدد الصفوف.
7. **تعدد المدارس + التقمّص:** أي عملية تُحدّد المدرسة يجب أن تستخدم **المدرسة الفعّالة** (`active_school_id` عند تقمّص المشرف) عبر API خادمي، لا `schools.single()` من المتصفح.
8. **الحراسة طبقتان:** واجهة (إخفاء) + خادم (رفض) + عزل المدرسة — لا تكتفِ بالواجهة.
9. **التصدير من الوحدات المشتركة:** استخدم `export const` (لا تشغل بالنا بـ function؛ المشكلة الحقيقية كانت الـ shadowing لا نوع التصدير).
10. **حقلا اسم مزدوجان في profiles:** `name_ar` (القوائم) و`full_name_ar` (الشريط الجانبي عبر PermissionsContext الذي يفضّله). أي تحديث للاسم يجب أن يحدّث **كليهما** — وإلا ظهر اسمان مختلفان في الواجهة. (أُصلح نموذج تعديل المستخدم 2026-06-11.)
11. **لا تُعِد تشغيل الترحيل 010:** يمنح `is_super_admin` بناءً على حالة لحظية (أقدم ملف + دور admin) — إعادة تشغيله = تصعيد صلاحيات دائم لحساب خاطئ.
12. **`position: sticky` داخل حاوية `overflow-auto`:** لا يعمل إن كان التمرير الفعلي يحدث في حاوية أخرى (body مع `min-h-screen`) — sticky يلتصق بأقرب سلف له overflow حتى لو لم يتمرر. الحل: App Shell (`h-screen overflow-hidden` + التمرير حصراً داخل main).
13. **الحذف الناعم من المتصفح مستحيل مع RLS يخفي المحذوف (مثبت مخبرياً):** PostgreSQL يفرض شرط USING لسياسة **القراءة (SELECT)** على الصف الجديد بعد UPDATE — فما دامت القراءة تتطلب `deleted_at IS NULL` (وهذا ضروري)، أي حذف ناعم من العميل يُرفض بـ"new row violates RLS" مهما كانت سياسة UPDATE. تفكيك السياسات (028) لا يكفي. **الحل الوحيد: حذف ناعم عبر API خادمي (service role)** — نمط `/api/tasks/[taskId]` و`/api/plans/[planId]`.

### ترحيل 029 (اعتماد الخطة) — ✅ شُغّل 2026-06-13
- **الميزة:** عمودا `approved_at` و`approved_by` على جدول plans.
- **المنطق:** الخطة المعتمدة محمية من الحذف (API يُرجع 403)؛ فقط الأرشفة متاحة. إلغاء الاعتماد يُعيد إمكانية الحذف. زر الاعتماد/الإلغاء حصري لـ `is_super_admin` (واجهة + API).
- **الواجهة:** شارة "معتمدة" على بطاقة الخطة وصفحة التفاصيل. قائمة ⋮ تعرض "اعتماد الخطة" / "إلغاء الاعتماد" للمشرف العام. زر الحذف مخفي للخطط المعتمدة.
- **إصلاح إضافي:** قائمة ⋮ كانت تُقصّ بسبب `overflow-hidden` على بطاقة الخطة → نُزيل `overflow-hidden` من البطاقة الخارجية ونُضيف `rounded-t-2xl/rounded-b-2xl` على العناصر الداخلية.

---

## 🧭 قرارات منتج مؤجلة (لا تُنفَّذ دون قرار صريح)
- **سياسة دورة حياة الخطط:** أرشفة تلقائية لخطط الأعوام الدراسية المنتهية عند بدء عام جديد (مع إشعار لمدير المدرسة وإمكانية الاستثناء/إلغاء الأرشفة يدوياً). الوضع الحالي (2026-06-12): الأرشفة يدوية بحتة عبر قائمة ⋮ على بطاقة الخطة ("أرشفة الخطة")، ومستقلة تماماً عن الحذف الناعم.
- **قفل بنية الخطة المعتمدة:** يمكن لاحقاً منع إضافة/حذف عقد الخطة المعتمدة (المحاور والأهداف). قرار منفصل لا يُنفَّذ الآن.

---

## 🗂️ قائمة الانتظار — تحسينات مقترحة (Backlog) — أُضيفت 2026-06-13

> أفكار متفق على قيمتها، غير منفّذة بعد. مرتّبة بالأولوية المقترحة.

### 1) الدليل المشترك (نهج كوجنيا) — أولوية عالية
ربط **نفس الدليل بعدة مهام** دون إعادة رفعه. حالياً الدليل يتبع مهمة واحدة (`evidence.task_id`).
- **القيمة:** يقلّل التكرار والتخزين، ويعكس الواقع (وثيقة واحدة تُثبت عدة مؤشرات).
- **التصميم المقترح:** جدول ربط `evidence_links (evidence_id, task_id)` (متعدد-لمتعدد) مع إبقاء `task_id` كـ"المهمة الأصلية" للتوافق؛ أو فصل الدليل عن المهمة كلياً وربطه عبر جدول الربط. يتطلب: واجهة "إرفاق دليل موجود" + تحديث الترقيم/العرض + سياسات RLS للربط.
- **اعتبار:** إعادة ترقيم الأدلة (المنفّذة) تفترض دليلاً لمهمة واحدة — تحتاج مراجعة عند التطبيق.

### 2) ميزة حِمل العمل (لإكمال منع التضارب) — أولوية متوسطة
استكمال التحذير الناعم بدقة السعة:
- إضافة **مدة/جهد المهمة** (ساعات) + **سعة الموظف اليومية**.
- فحص لحظة التكليف: تحذير عند تجاوز سعة الموظف في يوم/مدى (يمنع "10 مهام في يوم").
- لوحة حِمل عمل (heatmap لكل موظف).
- **ملاحظة:** لا توجد حقول ساعة حالياً؛ التحذير الناعم الحالي يعمل على تداخل التواريخ فقط.

### 3) ترشيح لوحة التجميع حسب نوع الخطة — أولوية منخفضة
إضافة مرشّح (تطوير مهني/دعم/متفوقين…) بجانب مرشّحَي القسم وحالة المهمة في `/dashboard/aggregate`.

### 4) تصدير قائمة المهام المتأخرة — أولوية منخفضة
زر تصدير (Excel/PDF) لقائمة المهام المطابقة لمرشّح الحالة في لوحة التجميع (خاصة المتأخرة).
- **(جزئياً 2026-06-15):** أُضيف تصدير CSV للوحة التجميع + تقرير «المهام المتأخرة» في مركز التقارير. يبقى تصدير PDF المباشر.

### 5) التوليد الخادمي لتقارير PDF (Server-side PDF generation) — أولوية متوسطة — أُضيف 2026-06-15
توليد PDF برمجياً على الخادم عبر **Headless Chromium (Puppeteer/Playwright)** فوق صفحات مركز التقارير الحالية، بدل «طباعة المتصفح → حفظ PDF».
- **القيمة:** إصدار بنقرة دون حوار طباعة · **الأتمتة** (تقارير مجدولة تُرسَل بالبريد) · ثبات المخرَج وترقيم الصفحات · **الأرشفة** كملفات ثابتة لملف الاعتماد · التوليد الجماعي (تقرير لكل قسم دفعة).
- **التكلفة:** أثقل على Vercel (حزمة `@sparticuz/chromium`، بدء بارد، حدود وقت/ذاكرة).
- **أنسب بداية:** «تقرير أداء شهري يُرسَل بالبريد آلياً» (أعلى قيمة). الطباعة الحالية تكفي للإصدار اليدوي الفوري.

### 6) مُحفِّز قاعدة لإلزام تواريخ التقويم المدرسي (DB trigger) — أولوية منخفضة-متوسطة — أُضيف 2026-06-15
حالياً منع بدء/انتهاء المهام في الأيام المحجوزة (عطلات «منع») مطبَّق على **الواجهة** في منافذ الإنشاء الثلاثة (إنشاء/تعديل/إضافة سريعة) — وهي كل مسارات الإنشاء الفعلية. الطبقة الخادمية (trigger على `tasks` يرفض التواريخ المحجوزة مع دعم تجاوز المدير عبر عمود `calendar_override`) مؤجَّلة كإحكام دفاعي إضافي.

### 7) عطلتا العيدين في التقويم — تذكير تشغيلي (بيانات لا كود) — أُضيف 2026-06-15
عيد الفطر وعيد الأضحى **غير مذكورين** في التقويم الرسمي القطري 2025/2026 الذي زُوّدنا به (يُعلَنان بحسب الرؤية). تُضاف تواريخهما **يدوياً** من «الإعدادات ← التقويم المدرسي» فور إعلان الوزارة.

---

## 🎯 الهدف
تحويل المهمة من "حالة يحدّدها المكلّف يدوياً" إلى **سير عمل مُقنَّن قائم على الأدلة والمراجعة**، يعطي مؤشراً حقيقياً على الإنجاز ويقوّي مصداقية تقرير QNSA (سجل موثّق: من رفع/اعتمد/قيّم ومتى).

## 🧩 النموذج المعتمد (مرجع التصميم)

**آلة حالات المهمة:**
```
لم تبدأ ──[المكلّف]──> جارية ──[المكلّف]──> مرفوعة للتقييم
                                              │ [المقيّم]
                          ┌───────────────────┴───────────────────┐
              معتمدة → منجزة + تقييم جودة (1-5)        مُعادة للتعديل (+سبب) → ترجع "جارية"
```
- **المكلّف يملك:** لم تبدأ · جارية · **مرفوعة للتقييم** (آخر ما يملكه).
- **المقيّم يملك:** اعتماد (→ منجزة + تقييم) · إعادة (+ سبب). يُمنع أن يكون المقيّم هو المكلّف.
- **متأخرة:** وسم **محسوب** (موعد فات وغير منجزة)، يُعرض فوق أي حالة — لا حالة مستقلة.
- **دورة الدليل:** مرفوع → (المقيّم) معتمد/مرفوض + سبب.
- **أنواع الأدلة المطلوبة:** اختيارية لكل مهمة (مصدرها القوائم المنسدلة). إن حُدّدت → لا إنجاز قبل اعتماد دليل لكل نوع مطلوب؛ وإلا فاعتماد المقيّم كافٍ.
- **شرط الإنجاز:** رفع المكلّف + (اعتماد الأدلة المطلوبة إن وُجدت) + اعتماد المقيّم + تقييم الجودة.

## 🗄️ تغييرات قاعدة البيانات (تُنفّذ في اليوم 1)
- `tasks.status`: قيم جديدة `submitted` (مرفوعة) و`returned` (مُعادة)؛ إيقاف استخدام `delayed` كحالة (يُحسب كوسم).
- `tasks`: + `submitted_at`, `submitted_by`, `return_note`, `required_evidence_types TEXT[]`.
- `evidence`: + `status` (pending/accepted/rejected) + `review_note` + `evidence_type` + `reviewed_by` + `reviewed_at`.
- جدول جديد `task_transitions` (سجل التحوّلات: task_id, from, to, actor_id, note, at) — مع RLS بالمدرسة.
- `dropdown_options`: فئة جديدة `evidence_type` (بذور: خطة · محضر اجتماع · تقرير · صور · مستند · نتائج).
- ترحيل البيانات: `delayed` الحالية → `in_progress`؛ الأدلة الحالية → `accepted` (لئلا تتعطّل المهام المنجزة).

---
---

## 🟦 الأسبوع الأول — قلب سير العمل (Phase 1)

### اليوم 1 | التصميم + ترحيل قاعدة البيانات ✅ 2026-06-09
- [x] مستند تصميم `docs/TASK_WORKFLOW.md` (آلة الحالات + الصلاحيات + الحالات الحدّية)
- [x] `migrations/021_task_workflow.sql` (الحقول + جدول التحوّلات + فئة evidence_type + ترحيل البيانات)
- [x] `STATUS_META`: إضافة submitted/returned + `OVERDUE_META` + `isOverdue()` في `lib/constants/tasks.ts`
- [ ] **يدوي:** تشغيل `migrations/021` في Supabase

### اليوم 2 | منطق الانتقالات + الصلاحيات (الخادم) ✅ 2026-06-09
- [x] `api/tasks/[taskId]/transition` (submit / approve / return):
  - عزل بالمدرسة (node→plan→school) + تحديد المكلّف (مباشر أو عبر الفريق)
  - المكلّف يرفع؛ المقيّم/صاحب rate_tasks يعتمد/يعيد؛ **منع التقييم الذاتي**
  - بوابات: الأدلة المطلوبة مرفوعة عند الرفع، ومعتمدة عند الاعتماد
  - تسجيل كل انتقال في `task_transitions` + إشعار الطرف الآخر
  - المقيّم الفعّال: reviewer_id أو fallback لصاحب rate_tasks/all

### اليوم 3 | شاشات المكلّف والمقيّم (صفحة المهمة) ✅ 2026-06-09
- [x] إضافة إجراء `start` للـ API (بدء العمل)
- [x] لوحة المكلّف: "بدء العمل" + "رفع للتقييم" (بدل تحديد "منجزة" يدوياً)
- [x] لوحة المقيّم: اعتماد (+تقييم 1-5) / إعادة (+سبب) — مرتبطة بالحالة `submitted`
- [x] عرض الحالة + وسم التأخير المحسوب + سبب الإعادة
- [ ] (يتبقّى Day 5) منع التعديل الحرّ للحالة في List/Kanban/my-tasks

### اليوم 4 | الإشعارات + سجل التحوّلات ✅ 2026-06-10
- [x] الإشعارات (نُفّذت في اليوم 2: رفع→المقيّم، اعتماد/إعادة→المكلّف)
- [x] عرض **سجل التحوّلات** في صفحة المهمة (الإجراء + المنفّذ + التاريخ + السبب) — يحفظ كل دورة
- [x] إظهار **سبب الإعادة** (إضافة return_note لجلب الصفحة)
- [x] **منع المكلّف من حذف المهمة** (الحذف للمدير أو منشئ المهمة فقط)

### اليوم 5 | "متأخرة" وسماً + تحديث العروض ✅ 2026-06-10
- [x] إزالة "delayed" كحالة من STATUS_META (صارت وسماً محسوباً via isOverdue/OVERDUE_META)
- [x] `cron/update-delayed`: توقّف عن الكتابة فوق status — يُشعر فقط (فلتر ≠ completed)
- [x] "كل المهام": الحالة **للعرض فقط** (أُزيلت القائمة المنسدلة الحرّة)
- [x] "مهامي": أزرار الحالة الحرّة → رابط "فتح المهمة لإدارة الحالة" + إصلاح خطأ team_members (user_id→profile_id)
- [x] Kanban: أعمدة جديدة (مرفوعة/مُعادة بدل متأخرة) + تعطيل تغيير الحالة بالسحب (سير العمل من صفحة المهمة)

---
---

## 🟩 الأسبوع الثاني — الأدلة (Phase 2 + 3)

> **حالة فعلية (2026-06-13):** انحرف التنفيذ عن قائمة الأيام أدناه. بُنيت بدلاً منها **حزمة أدلة موسّعة** (فيديو يوتيوب + ملفات متعددة + تعديل الدليل + صفحة غلاف/طباعة ذكية + ترقيم تلقائي) إضافةً إلى ميزات خارج النطاق (اعتماد الخطة، الأماكن، التحذير الناعم، لوحة التجميع) — انظر **سجل الإنجازات**.
> **ما زال معلّقاً من هذا الأسبوع:** تحقّق الدليل لكل دليل (يوم 6) · أنواع الأدلة عند الرفع (يوم 7) · بوابة الإنجاز (يوم 8) · صفحة `/dashboard/evidence` الموحّدة (يوم 9) · سجل الاعتماد في تقرير QNSA (يوم 10).

### اليوم 6 | تحقّق الدليل (اعتماد/رفض)
- [ ] ضمن لوحة المقيّم: اعتماد/رفض كل دليل + سبب الرفض
- [ ] عرض حالة كل دليل (مرفوع/معتمد/مرفوض) للمكلّف

### اليوم 7 | أنواع الأدلة + التصنيف عند الرفع
- [ ] فئة `evidence_type` في الإعدادات (القوائم المنسدلة) + بذورها
- [ ] حقل "أنواع الأدلة المطلوبة" عند إنشاء/تعديل المهمة (اختياري)
- [ ] عند رفع الدليل: اختيار نوعه

### اليوم 8 | بوابة الإنجاز
- [ ] منع الاعتماد/الإنجاز قبل توفّر دليل معتمد لكل نوع مطلوب
- [ ] مؤشر بصري في المهمة: الأدلة المطلوبة وحالة كلٍّ منها

### اليوم 9 | صفحة إدارة الأدلة
- [ ] `/dashboard/evidence`: قائمة موحّدة بكل الأدلة (فلترة بالحالة/النوع/المهمة) + تنزيل + مراجعة سريعة

### اليوم 10 | التقارير + تقرير QNSA
- [ ] تحديث التقارير لتعكس الحالات الجديدة (مرفوعة/مُعادة/منجزة)
- [ ] تقرير QNSA: إظهار سجل الاعتماد (من اعتمد/قيّم ومتى) لتعزيز المصداقية
- [ ] Build + Push

---
---

## 🟢 الأسبوع الثالث — الإطلاق (مُرحّل من الخطة السابقة)

### اليوم 11 | اختبار شامل نهائي (كان يوم 59)
- [ ] اختبار التدفق الكامل بأدوار مختلفة (مكلّف/مقيّم/مدير)
- [ ] اختبار الحالات الحدّية (بلا مقيّم، بلا أدلة، إعادة متكررة)
- [ ] اختبار الموبايل + المتصفحات
- [ ] إصلاح ما يظهر

### اليوم 12 | التوثيق + الإطلاق (كان يوم 60)
- [ ] تحديث `PROJECT_CONTEXT.md` بالنموذج الجديد
- [ ] تحديث التوثيق + تواريخ الإنجاز
- [ ] Build نهائي + Push + التحقق من Vercel
- [ ] 🎉 الإطلاق

---
---

## 🟪 المرحلة الثالثة — التحسينات المعتمدة (Backlog → أيام عمل) — مُضافة 2026-06-13

> أيام عمل مجدولة للأفكار المتفق عليها (مصدرها قسم «قائمة الانتظار» أعلاه). الترتيب بالأولوية.

### اليوم 13 | الدليل المشترك (نهج كوجنيا) — أولوية عالية
- [ ] ترحيل: جدول ربط `evidence_links (evidence_id, task_id)` متعدد-لمتعدد + RLS (عبر المهمة→الخطة→المدرسة)
- [ ] واجهة "إرفاق دليل موجود" على المهمة (اختيار من أدلة المدرسة دون إعادة رفع)
- [ ] تحديث عرض الأدلة/الترقيم ليعمل مع الدليل المرتبط بعدة مهام
- [ ] مراجعة منطق إعادة الترقيم (يفترض حالياً دليلاً لمهمة واحدة)

### اليوم 14 | حِمل العمل — الجزء 1 (الحقول) — أولوية متوسطة
- [ ] ترحيل: `tasks.effort_hours` + سعة الموظف اليومية (`profiles.daily_capacity_hours` أو إعداد عام)
- [ ] إدخال مدة المهمة في إنشاء/تعديل المهمة
- [ ] عرض الجهد/السعة في صفحة المهمة

### اليوم 15 | حِمل العمل — الجزء 2 (الفحص واللوحة)
- [ ] فحص لحظة التكليف: تحذير عند تجاوز سعة الموظف في يوم/مدى (يكمّل التحذير الناعم)
- [ ] لوحة حِمل عمل (heatmap لكل موظف عبر التواريخ)
- [ ] دمج الفحص في إنشاء المهمة وصفحة المهمة

### اليوم 16 | تحسينات لوحة التجميع — أولوية منخفضة
- [ ] ترشيح لوحة التجميع حسب **نوع الخطة** (بجانب القسم وحالة المهمة)
- [ ] **تصدير** قائمة المهام المطابقة (المتأخرة خاصةً) إلى Excel/PDF
- [ ] Build + Push

---

## سجل الإنجازات (المرحلة الثانية)
| التاريخ | اليوم | المنجز | الملفات |
|---------|-------|--------|---------|
| 2026-06-10 | أمان | حصر التكليف/التعديل/الحذف بـ manage_tasks (واجهة) + **حارس خادمي** عبر `/api/tasks/[taskId]` (PATCH/DELETE) | `api/tasks/[taskId]/route`, taskId/page |
| 2026-06-10 | إصلاح | إنشاء الخطة عبر `/api/plans` يحسب المدرسة الفعّالة (إصلاح خطأ RLS لمشرف النظام/التقمّص) | `api/plans/route`, plans/new/page |
| 2026-06-11 | أمان | حذف المستخدمين عبر API خادمي (حماية الحسابات المميّزة/الذات + لا إزالة تفاؤلية) + `users/create` يحترم التقمّص وفحص manage_users + ترحيل 023 (منع DELETE على profiles من المتصفح — ✅ شُغّل) | `api/users/[userId]/route`, `api/users/create/route`, users/page, `migrations/023` |
| 2026-06-11 | ميزة | **قفل المهمة المنجزة** (3 طبقات): واجهة (إخفاء كل أدوات التعديل عدا التعليقات + لافتة قفل) + خادم (رفض PATCH/DELETE + إجراءا `reopen` بسبب إلزامي لصاحب manage_tasks و`request_reopen` إشعاراً لمشرف نظام المدرسة) + قاعدة (ترحيل 024 — **بانتظار تشغيله يدوياً**). التقييم السابق يُصفَّر عند إعادة الفتح ويبقى في سجل التحوّلات | `api/tasks/[taskId]/transition`, `api/tasks/[taskId]`, tasks/[taskId]/page, evidence/new/page, `migrations/024` |
| 2026-06-13 | ميزة | **اعتماد الخطة** (029): حماية الخطة المعتمدة من الحذف (أرشفة بدلاً منه) + قفل حذف عقدها (إضافة مسموحة) — حصري لمشرف النظام. إصلاح `insertBefore` المتكرر (عزل الأيقونة/النص) | `api/plans/[planId]/certify`, `api/plans/[planId]`, `api/plans/[planId]/nodes/[nodeId]`, plans/page, plans/[planId]/page, `migrations/029` |
| 2026-06-13 | ميزة | **أدلة الفيديو (يوتيوب)**: لصق رابط + صورة مصغّرة + QR + وصف بالذكاء الاصطناعي (Groq) + الترقيم التلقائي بعد الحذف | `api/ai/evidence-description`, evidence/new/page, tasks/[taskId]/page, `migrations/030` |
| 2026-06-13 | ميزة | **ملفات متعددة لكل دليل** (031) + **تعديل الدليل** (تسمية/إضافة/حذف مرفقات) + **صفحة غلاف/طباعة ذكية** (تسلسل هرمي + شعار المدرسة + اتجاه تلقائي + عزل عن واجهة التطبيق) + حذف الدليل خادمياً | `api/tasks/[taskId]/evidence/[evidenceId]`, evidence/new+edit, evidence/[id]/print, `migrations/031` |
| 2026-06-13 | ميزة | **الأماكن (موارد مكانية)** (032): كتالوج لكل مدرسة + ربط متعدد بالمهمة + إدارة في الإعدادات + **التحذير الناعم للتعارض** (مكان/موظف متداخل زمنياً) | `api/locations`, `components/LocationsManager`, `lib/conflicts`, `components/ConflictWarning`, tasks pages, `migrations/032` |
| 2026-06-13 | ميزة | **لوحة التجميع**: أبعاد الخطة (قسم/نوع/صاحب) + إشراف الأقسام (033) + صلاحية `view_aggregate` + تجميع مؤشرات الأقسام + مرشّح حالة المهام والتنقيب لكل خطة. العزل: مشرف=الكل، نائب=أقسامه المُسندة، منسق=قسمه | `api/aggregate`, aggregate/page, `api/department-supervisors`, `components/DeptSupervisorsManager`, `migrations/033` |
| | | | |
