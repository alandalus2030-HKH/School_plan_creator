# مقترحات التحسين الوظيفي — نظام متابعة الخطط المدرسية
**إصدار:** 1.0 | **تاريخ الإعداد:** 2026-06-06
**المرجع:** مقارنة مع Asana · Monday.com · ClickUp · Notion · Linear · Microsoft Planner · Perdoo · Databox

> **كيفية الاستخدام:** وضع ✅ + التاريخ أمام كل بند يُنجز.
> في أي جلسة عمل اكتب: "اقرأ PROJECT_CONTEXT.md و FUNCTIONAL_IMPROVEMENTS.md ثم [المهمة]"

---

## القسم الأول — ميزات مفقودة مقارنة بالمنافسين العالميين

---

### 1. البحث الشامل (Universal Search) 🔍
**الأولوية:** 🔴 عالية جداً
**المشكلة:** لا يوجد بحث شامل في النظام. المستخدم يجب أن يعرف أين توجد المهمة قبل أن يبحث عنها.
**المرجع العالمي:** Notion وClickUp — الضغط على `Ctrl+K` يفتح بحثاً شاملاً يجد المهمة أو الخطة أو الدليل أو المستخدم فوراً.
**الأثر:** المستخدم الذي لديه 200+ مهمة يضيع وقته في التنقل.

**المطلوب تقنياً:**
- شريط بحث في TopBar يعمل بـ `Ctrl+K`
- يبحث في: أسماء المهام + الخطط + العقد + المستخدمين + الأدلة
- نتائج فورية أثناء الكتابة (Debounced search)
- تصنيف النتائج: مهام / خطط / مستخدمون / أدلة
- الانتقال المباشر للنتيجة بنقرة

**الجداول المتأثرة في Supabase:** `tasks`, `plans`, `plan_nodes`, `profiles`, `evidence`

- [ ] تنفيذ Global Search

---

### 2. تحديث المهمة من القائمة المباشر (Inline Editing) ✏️
**الأولوية:** 🔴 عالية جداً
**المشكلة:** تغيير حالة مهمة يتطلب فتح صفحة كاملة وإعادة التحميل.

**المسار الحالي (5 خطوات):**
```
النقر على المهمة → انتظار تحميل الصفحة → تغيير الحالة → حفظ → العودة للقائمة
```

**المرجع العالمي:** Asana وLinear — النقر على الحالة في القائمة يفتح قائمة منسدلة، التغيير بنقرة واحدة.
**الأثر:** المشرف الذي يراجع 20 مهمة يقضي 10 دقائق بدل دقيقتين.

**المطلوب تقنياً:**
- تحويل حقل الحالة في قائمة المهام لزر تفاعلي
- Dropdown مباشر عند النقر على الحالة
- حفظ فوري بدون إعادة تحميل الصفحة (Optimistic Update)
- تأكيد بصري سريع (Flash أخضر عند الحفظ)
- نفس المنطق للأولوية والمكلَّف

- [ ] تنفيذ Inline Status Update في قائمة المهام
- [ ] تنفيذ Inline Priority Update
- [ ] تنفيذ Inline Assignee Update

---

### 3. عرض حِمْل العمل (Workload View) 📊
**الأولوية:** 🟠 عالية
**المشكلة:** لا توجد طريقة لمعرفة "من لديه عمل أكثر من طاقته؟"
**المرجع العالمي:** Monday.com — Workload view يُظهر كل موظف والعدد الفعلي لمهامه مقارنة بالطاقة الاستيعابية.
**الأثر:** مدير يُكلِّف 15 مهمة لمعلم واحد في نفس الأسبوع لن يعرف إلا بعد التأخر.

**المطلوب تقنياً:**
- إضافة تبويب "توزيع العمل" في صفحة التقارير أو الفرق
- عرض: اسم الموظف + عدد المهام النشطة + المهام المستحقة هذا الأسبوع + نسبة الإنجاز
- لون تحذيري (عنابي) لمن تجاوز 5 مهام نشطة في وقت واحد
- إمكانية إعادة التوزيع بالسحب والإفلات (Drag to Reassign)

- [ ] تنفيذ Workload View في صفحة الفرق أو التقارير

---

### 4. المهام الفرعية داخل المهمة (Subtasks) 📋
**الأولوية:** 🟠 عالية
**المشكلة:** الهرمية الموجودة تعمل على مستوى التخطيط، لكن داخل المهمة الواحدة لا توجد خطوات.
**المرجع العالمي:** Asana — كل مهمة تنقسم لـ subtasks، وكل subtask له مكلَّف وتاريخ مستقل.

**مثال واقعي:**
```
مهمة: إعداد تقرير الاعتماد
  ├── جمع البيانات من الأقسام     [أحمد] [01/10]
  ├── كتابة مسودة التقرير         [سارة] [15/10]
  ├── مراجعة المدير               [المدير] [20/10]
  └── رفع التقرير النهائي         [سارة] [25/10]
```

**المطلوب تقنياً:**
- إضافة جدول `subtasks` في قاعدة البيانات (task_id, name_ar, assignee_id, due_date, is_done)
- أو إضافة عمود `parent_task_id` في جدول tasks الحالي (أبسط)
- عرض الـ subtasks في صفحة تفاصيل المهمة مع Checkbox
- عداد Subtasks في قائمة المهام (3/5 مكتملة)

- [ ] تصميم بنية Subtasks في قاعدة البيانات
- [ ] بناء واجهة Subtasks في صفحة تفاصيل المهمة
- [ ] عرض تقدم Subtasks في قائمة المهام

---

### 5. قواعد الأتمتة (Automations) ⚡
**الأولوية:** 🟠 عالية
**المشكلة:** كل شيء يدوي — تغيير الحالة، الإشعارات، تحديث التأخير.
**المرجع العالمي:** ClickUp وMonday.com — قواعد "إذا/ثم" بدون برمجة.

**الأتمتة المقترحة للنظام:**
```
إذا انتهى موعد المهمة ولم تكتمل      → غيّر حالتها لـ "متأخرة" تلقائياً
إذا اكتملت مهمة                      → أشعر المراجع تلقائياً
إذا اقترب الموعد بـ 48 ساعة          → أرسل تذكير للمكلَّف
إذا وُكِّلت مهمة لشخص               → أشعره فوراً
إذا اكتملت كل مهام خطة              → أشعر المدير
```

**المطلوب تقنياً:**
- Supabase pg_cron job يعمل يومياً لتحديث المهام المتأخرة
- Vercel Cron للإشعارات التلقائية
- جدول `automation_rules` (اختياري للمرحلة المتقدمة)

- [ ] إعداد Cron job يومي لتحديث حالة المهام المتأخرة تلقائياً
- [ ] إشعار تلقائي عند اقتراب الموعد بـ 48 ساعة
- [ ] إشعار تلقائي للمراجع عند اكتمال المهمة

---

### 6. تعليقات بـ @ذكر (Mentions in Comments) 💬
**الأولوية:** 🟡 متوسطة
**المشكلة:** التعليقات موجودة لكن بدون توجيه — المدير يكتب تعليقاً ولا أحد يعلم أنه موجه له.
**المرجع العالمي:** Notion وLinear — كتابة `@اسم_الشخص` يُرسل إشعاراً مباشراً له.

**المطلوب تقنياً:**
- Autocomplete عند كتابة `@` في حقل التعليق يعرض قائمة المستخدمين
- استخراج المُذكورين من نص التعليق عند الحفظ
- إرسال إشعار لكل مُذكور تلقائياً
- تمييز الأسماء المُذكورة بلون مختلف في عرض التعليق

- [ ] تنفيذ @Mentions في نظام التعليقات

---

### 7. عرض التقويم الموحّد (Calendar View) 📅
**الأولوية:** 🟠 عالية
**المشكلة:** لا يوجد عرض تقويمي للمهام — Gantt Chart موجود لكنه للتخطيط، ليس للعمل اليومي.
**المرجع العالمي:** Asana وClickUp — تقويم شهري/أسبوعي يُظهر المهام على الأيام.
**الأثر:** المدير لا يستطيع رؤية "ماذا يحدث هذا الأسبوع" بنظرة واحدة.

**المطلوب تقنياً:**
- إضافة تبويب "التقويم" في صفحة كل المهام
- عرض المهام حسب تاريخ الاستحقاق (end_date) على شبكة أيام
- ألوان حسب الحالة
- إمكانية السحب لتغيير الموعد (Drag to Reschedule)
- مكتبة مقترحة: `react-big-calendar` أو `@fullcalendar/react`

- [ ] تنفيذ Calendar View في صفحة المهام

---

### 8. النشرة اليومية بالبريد (Daily/Weekly Digest) 📧
**الأولوية:** 🟡 متوسطة
**المشكلة:** لا يوجد ملخص دوري — المستخدم يجب أن يفتح التطبيق ليعرف وضعه.
**المرجع العالمي:** Asana — بريد صباحي يحتوي: مهامك المستحقة اليوم + ما تأخر + ما أُنجز أمس.

**محتوى الملخص المقترح:**
```
صباح الخير [اسم المستخدم]

📌 مهامك المستحقة اليوم: 3 مهام
⚠️ مهام متأخرة: 1 مهمة [اضغط هنا]
✅ أُنجز أمس في فريقك: 4 مهام

[فتح النظام ←]
```

**المطلوب تقنياً:**
- Vercel Cron يعمل كل صباح 7:00 بتوقيت قطر
- Resend API لإرسال البريد
- إعداد في ملف profile لتفعيل/إيقاف الإشعارات البريدية

- [ ] تصميم قالب بريد HTML للنشرة اليومية
- [ ] إعداد Cron job للإرسال الصباحي
- [ ] إضافة إعداد تفضيل المستخدم (يومي/أسبوعي/إيقاف)

---

### 9. سجل النشاط (Activity Feed) 🕐
**الأولوية:** 🟡 متوسطة
**المشكلة:** لا يوجد سجل مرئي لـ "ماذا حدث في النظام اليوم".
**المرجع العالمي:** Notion وLinear — قائمة كرونولوجية: "أحمد غيّر حالة مهمة X · منذ ساعتين".

**المطلوب تقنياً:**
- تفعيل جدول `audit_logs` الموجود في schema وتعبئته عند كل تغيير
- إضافة ودجة "آخر النشاطات" في Dashboard
- عرض: الفاعل + الإجراء + العنصر المتأثر + الوقت النسبي (timeAgo)
- فلتر: نشاطي فقط / نشاط الفريق / كل النشاط

- [ ] تفعيل تسجيل النشاط في audit_logs عند التغييرات الرئيسية
- [ ] بناء ودجة Activity Feed في Dashboard

---

### 10. لوحة تحكم مخصصة لكل دور (Role-Based Dashboard) 🎛️
**الأولوية:** 🟡 متوسطة
**المشكلة:** لوحة التحكم ثابتة — المدير والمعلم يرون نفس الواجهة.
**المرجع العالمي:** Monday.com — كل مستخدم يُخصص لوحته.

**التخصيص المقترح حسب الدور:**

| الدور | ما يجب أن يرى |
|-------|--------------|
| مدير المدرسة | نسبة إنجاز كل الخطط + المتأخرات + أداء الأقسام |
| منسق الخطة | تقدم خطته + مؤشرات الأداء + المهام المعلقة |
| قائد الفريق | مهام فريقه + الأعضاء المتأخرون |
| معلم/منفذ | مهامي اليوم + المواعيد القادمة فقط |

- [ ] إنشاء dashboard components مختلفة حسب الدور
- [ ] ربطها بـ PermissionsContext الموجود

---

## القسم الثاني — وظائف حالية تحتاج تبسيطاً

---

### التبسيط الأول — إنشاء المهمة: Quick Add Button

**المسار الحالي (معقد):**
```
الخطط ← اختيار خطة ← اختيار محور ← اختيار عقدة ← "مهمة جديدة" ← نموذج كامل
= 5 خطوات قبل الكتابة
```

**المسار المقترح (Asana Model):**
```
زر [+] العائم في أي صفحة ← Quick Modal صغير ← Enter
= خطوة واحدة
```

**تصميم Quick Add Modal:**
```
┌─────────────────────────────────────┐
│  ✏️  اسم المهمة                     │
│  ────────────────────────────────── │
│  تحت: [اختر خطة/محور ▼]            │
│  لـ: [اختر شخص ▼]   الموعد: [📅]   │
│                                     │
│  [إضافة سريعة]  [تفاصيل أكثر ←]    │
└─────────────────────────────────────┘
```

**التنفيذ:**
- زر `+` ثابت في أسفل يمين الشاشة (Fixed Floating Button)
- اختصار لوحة مفاتيح: `N` لفتحه من أي صفحة
- الحقول الإلزامية فقط: الاسم (الباقي اختياري)
- "تفاصيل أكثر" يفتح صفحة إنشاء المهمة الكاملة مع المعلومات المُدخلة

- [ ] تصميم وتنفيذ Quick Add Floating Button
- [ ] إضافة اختصار لوحة المفاتيح `N`

---

### التبسيط الثاني — الهرمية العميقة

**المشكلة:** النظام يدعم حتى 6 مستويات + المهام = 7 مستويات متداخلة.
المستخدم العادي يضيع في التنقل بين المستويات.

**المرجع العالمي:** ClickUp تدعم التعشيش اللانهائي لكنها تُخفيه — المستخدم لا يرى إلا المستوى الذي هو فيه.

**الحلول المقترحة (لا تغيير في البنية — فقط واجهة):**
- إضافة **Breadcrumb واضح** في أعلى كل صفحة عقدة/مهمة:
  ```
  الخطة الدراسية 2025 ← محور الحوكمة ← مبادرة التخطيط ← هدف التوثيق
  ```
- **"عرض مدمج"** افتراضي يُظهر جميع المهام تحت الخطة بغض النظر عن مستواها
- **مؤشر المستوى** بلون مختلف لكل عمق (موجود جزئياً في NodeRow)
- إضافة "انتقل لأعلى مستوى" (Parent) بزر واضح

- [ ] إضافة Breadcrumb في صفحات العقد والمهام
- [ ] إضافة عرض "كل المهام" مدمجاً تحت الخطة بدون تصنيف هرمي

---

### التبسيط الثالث — نموذج KPIs

**المشكلة:** النموذج الحالي يطلب مصطلحات تقنية لا يفهمها المعلم العادي:
- الخط الأساسي (Baseline)
- نوع المؤشر (أثر/نتيجة/مخرج) — فروق أكاديمية دقيقة
- تكرار القراءة
- إدخال القراءات يدوياً

**المرجع العالمي:** Perdoo (أداة OKR) — يسأل 3 أسئلة فقط:
1. ماذا تريد أن تحقق؟
2. كيف تقيس النجاح؟ (من رقم X إلى رقم Y)
3. متى؟

**الحل المقترح — وضعان:**

```
[وضع بسيط ✓] ──────────────────
اسم المؤشر: [نسبة حضور الطلاب     ]
الهدف:      من [75%] إلى [90%]
الموعد:     [نهاية الفصل الأول    ]

                    [وضع متقدم ▼]
```

```
[وضع متقدم] ────────────────────
+ نوع المؤشر / تكرار القراءة / الخط الأساسي
(للمتخصصين فقط)
```

- [ ] إعادة تصميم نموذج KPI بوضعين (بسيط/متقدم)
- [ ] تغيير الوضع الافتراضي للبسيط

---

### التبسيط الرابع — صفحة التقارير

**المشكلة:** صفحة التقارير تعرض 5 تبويبات + فلاتر متعددة + رسوم بيانية + جداول في وقت واحد.
**الأثر:** المستخدم يشعر بالإرهاق ولا يعرف من أين يبدأ.

**المرجع العالمي:** Databox — يبدأ بسؤال "ماذا تريد أن تعرف اليوم؟"

**الصفحة المقترحة (3 مستويات):**

```
المستوى 1 — اللمحة السريعة (افتراضي):
┌──────────────────────────────────────┐
│ نسبة الإنجاز هذا الشهر      [67%]   │
│ ████████████░░░░░░░░░░               │
│                                      │
│ المتأخرة: 4    المنجزة: 13    الكل: 20│
│                                      │
│ [تقرير مفصل ←]  [طباعة 🖨️]          │
└──────────────────────────────────────┘

المستوى 2 — تقرير مفصل (بنقرة):
الوضع الحالي للتبويبات (نظرة عامة، هيكل، KPIs، أفراد، متأخرات)

المستوى 3 — تصدير:
Excel / PDF / طباعة
```

- [ ] إضافة "لمحة سريعة" كصفحة افتراضية للتقارير
- [ ] نقل التبويبات التفصيلية لما بعد النقر على "تقرير مفصل"

---

### التبسيط الخامس — صفحة الاجتماعات

**المشكلة:** الاجتماعات وحدة منعزلة تماماً — لا ربط بالمهام التي نوقشت، ولا قرارات تتحول لمهام.

**المرجع العالمي:** Notion — كل اجتماع صفحة تحتوي: الأجندة + ملاحظات + قرارات تتحول لمهام بنقرة.

**الإضافات المقترحة:**
- حقل "مهام نتجت عن الاجتماع" مع زر "أنشئ مهمة من القرار"
- ربط الاجتماع بالخطة أو المحور المرتبط
- بعد الاجتماع: إرسال ملخص تلقائي بالبريد للحاضرين
- إضافة حقل "محضر الاجتماع" (نص حر)

- [ ] إضافة حقل "القرارات والمهام الناتجة" في نموذج الاجتماع
- [ ] زر "أنشئ مهمة" مباشرة من صفحة الاجتماع
- [ ] إرسال ملخص بريدي للحاضرين بعد الاجتماع

---

## القسم الثالث — Quick Wins (تحسينات سريعة التأثير)

> تغييرات لا تحتاج معمارية جديدة — واجهة فقط، تنفيذ سريع وأثر كبير.

| # | التحسين | الوقت المقدر | الأثر | الحالة |
|---|---------|-------------|-------|--------|
| 1 | تغيير الحالة Inline في قائمة المهام | يومان | ⬆⬆⬆ عالي جداً | - [ ] |
| 2 | زر Quick Add عائم في كل الصفحات | يوم | ⬆⬆⬆ عالي جداً | - [ ] |
| 3 | شريط بحث شامل في TopBar | يومان | ⬆⬆⬆ عالي جداً | - [ ] |
| 4 | Breadcrumb واضح في صفحات العقد والمهام | نصف يوم | ⬆⬆ عالي | - [ ] |
| 5 | حفظ الفلاتر في localStorage (تبقى بعد إعادة التحميل) | يوم | ⬆⬆ عالي | - [ ] |
| 6 | عداد المهام المستحقة اليوم في Sidebar بجانب "مهامي" | ساعة | ⬆⬆ عالي | - [ ] |
| 7 | تأكيد بصري فوري عند تغيير الحالة (Flash أخضر) | ساعتان | ⬆⬆ عالي | - [ ] |
| 8 | تقليص نموذج KPI لـ 3 حقول أساسية افتراضياً | يوم | ⬆⬆ عالي | - [ ] |
| 9 | إضافة "عرض التقويم" كتبويب في صفحة المهام | 3 أيام | ⬆⬆⬆ عالي جداً | - [ ] |
| 10 | اختصار لوحة المفاتيح `N` لإنشاء مهمة سريعة | ساعة | ⬆⬆ عالي | - [ ] |

---

## القسم الرابع — خريطة التطوير الزمنية

```
الشهر الأول — أعلى أثر:
├── ✦ Quick Add Floating Button
├── ✦ Inline Status Update في قائمة المهام
├── ✦ Global Search (بحث شامل)
├── ✦ Breadcrumb في صفحات العقد
└── ✦ عداد مهامي اليوم في Sidebar

الشهر الثاني — تحسين التجربة:
├── Calendar View للمهام
├── تبسيط نموذج KPI
├── Workload View في التقارير/الفرق
├── حفظ الفلاتر
└── تبسيط صفحة التقارير (لمحة سريعة)

الشهر الثالث — تعمّق الوظائف:
├── @Mentions في التعليقات
├── Subtasks داخل المهمة
├── Activity Feed في Dashboard
└── ربط الاجتماعات بالمهام

الشهر الرابع وما بعده — الأتمتة والذكاء:
├── Cron Jobs للإشعارات التلقائية
├── Daily Digest بريد صباحي
├── لوحة تحكم مخصصة لكل دور
└── Role-Based Dashboard
```

---

## القسم الخامس — الثغرة الاستراتيجية المحورية

> كل الأدوات العالمية الناجحة تتبع مبدأً واحداً:

**"الوصول للمعلومة الصحيحة في ثلاث نقرات أو أقل"**

النظام الحالي ممتاز في **التخزين والتنظيم** — لكنه يحتاج تطويراً في **الوصول السريع**.

المستخدم اليومي (المعلم، المنسق) يريد إجابة على ثلاثة أسئلة فقط:
1. **ما مهامي اليوم؟** — الإجابة موجودة في "مهامي" لكن تحتاج نقرات
2. **كيف أُحدّث حالة مهمة بسرعة؟** — يحتاج فتح صفحة كاملة حالياً
3. **هل أنا متأخر في شيء؟** — لا يوجد تنبيه تلقائي

**إذا أجبنا على هذه الأسئلة الثلاثة بنقرتين أو ثلاث — النظام جاهز للتسويق.**

---

## سجل التنفيذ

| التاريخ | التحسين المُنفَّذ | الملفات المتأثرة | المنفذ |
|---------|-----------------|-----------------|--------|
| | | | |

---

## ملاحظة للمطور

> هذا الملف مكمّل لـ `IMPROVEMENT_PLAN.md` الذي يركز على الجودة التقنية والأمان.
> هذا الملف يركز على **الوظائف والتجربة** فقط.
> عند تنفيذ أي بند: ضع ✅ + التاريخ + اسم الملفات المُعدَّلة في سجل التنفيذ أعلاه.
