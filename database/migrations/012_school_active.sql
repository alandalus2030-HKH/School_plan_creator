-- ============================================================
-- الترحيل 012: تفعيل/تعطيل المدارس
-- ============================================================
-- التاريخ: 2026-06-07
-- الغرض: تعطيل مدرسة انتهى تعاقدها دون حذف بياناتها
--        (المعيار العالمي: Deactivate بدل Delete للحسابات ذات البيانات)
--
-- مدرسة معطَّلة → مستخدموها لا يستطيعون الدخول
-- ============================================================

ALTER TABLE schools
  ADD COLUMN IF NOT EXISTS is_active BOOLEAN NOT NULL DEFAULT true;

-- التحقق
SELECT id, name_ar, is_active FROM schools ORDER BY created_at;
