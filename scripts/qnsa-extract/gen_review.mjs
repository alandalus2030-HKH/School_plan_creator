import fs from 'fs'
import path from 'path'
import { fileURLToPath } from 'url'
const DIR = path.dirname(fileURLToPath(import.meta.url))
const ROOT = path.resolve(DIR, '../..')                      // جذر المستودع
const SP = process.env.QNSA_WORK || path.join(DIR, 'work')   // مجلّد الوسائط
const c = JSON.parse(fs.readFileSync(SP + '/catalog.json', 'utf8'))
const flags = JSON.parse(fs.readFileSync(SP + '/flags.json', 'utf8'))
const merges = JSON.parse(fs.readFileSync(SP + '/merges.json', 'utf8'))

/* ملخّص سلم التقدير — يُحسب من البيانات لا يُكتب يدوياً */
const rub = JSON.parse(fs.readFileSync(SP + '/rubric.json', 'utf8'))
const rflags = JSON.parse(fs.readFileSync(SP + '/rubric_flags.json', 'utf8'))
const RUB = {
  levels: rub.levels,
  descCount: rub.groups.reduce((n, g) => n + g.rows.length, 0),
  phrases: rub.groups.reduce((n, g) =>
    n + g.rows.reduce((k, r) => k + [1, 2, 3, 4, 5].filter(l => r[l]).length, 0), 0),
  byLevel: rub.groups.reduce((a, g) => {
    g.rows.forEach(r => [1, 2, 3, 4, 5].forEach(l => { if (r[l]) a[l] = (a[l] || 0) + 1 }))
    return a
  }, {}),
  exact: rflags.report.filter(r => r.ok).length,
  merged: rflags.merged.length,
  votes: rflags.votes.length,
  votesWrap: rflags.votes.filter(v => v.decision === 'wrap').length,
  votesSplit: rflags.votes.filter(v => v.decision === 'split').length,
  unterminated: rflags.integrity.unterminated.length,
  missing: rflags.integrity.missingLevels.length,
  over: rflags.report.filter(r => r.indicators !== null && r.rubricRows > r.indicators).length,
}
const esc = s => String(s).replace(/\|/g, 'ǀ').replace(/\n/g, ' ')
const totalInd = c.subs.reduce((n, s) => n + s.indicators.length, 0)
const textEdits = JSON.parse(fs.readFileSync(SP + '/text_edits.json', 'utf8'))
const audit = JSON.parse(fs.readFileSync(SP + '/audit_counts.json', 'utf8'))
const NODES = c.standards.length + c.aspects.length + c.subs.length + totalInd + 0
const EDITS = c.subs.filter(s => s.decided && s.summaryName && s.code === '3.1.8').length + textEdits.length
const corrected = Object.entries(audit.counts).filter(([, v]) => v.corrected.length)

/* نماذج الأدلة — تُحسب من البيانات لا تُكتب يدوياً */
const evid = JSON.parse(fs.readFileSync(SP + '/evidence.json', 'utf8'))
const evFlags = JSON.parse(fs.readFileSync(SP + '/evidence_flags.json', 'utf8'))
const EV = {
  total: evid.groups.reduce((n, g) => n + g.items.length, 0),
  joins: evFlags.joins.length, splits: evFlags.splits.length, lost: evFlags.lostMarks.length,
}
const aspectName = new Map(c.aspects.map(a => [a.code, a.name]))
const catalog_aspect = code => aspectName.get(code) || ''

let md = `# كتالوج QNSA النهائي — تقرير الاستخراج والمراجعة

**المصدر:** \`دليل الاعتماد نهائي.docx\` (القسم الثاني: معايير الاعتماد المدرسي الوطني)
**التاريخ:** 2026-09-15 (بعد التدقيق اليدويّ وحسم الصياغات)
**الترحيلات:** \`064_qnsa_final_framework.sql\` (الشجرة) · \`065_qnsa_rubric.sql\` (سلم التقدير) · \`066_qnsa_wording_decisions.sql\` (الصياغات) · \`067_qnsa_evidence_samples.sql\` (الأدلة)

## 0) ✅ التدقيق اليدويّ للمستخدم (${audit.audited}) — طُبِّق وصار مرجعاً دائماً

راجع المستخدم بالعين عدد المؤشرات والأوصاف لكل معيار فرعي مقابل الوثيقة، وصحّح
**${corrected.length} مواضع** — كلّها كانت أخطاء في الاستخراج لا في الوثيقة:

| المعيار | ما صُحِّح | قبل | بعد | السبب |
|---|---|---|---|---|
| 1.1.2 | مؤشرات | 2 | 3 | بندٌ سقطت علامة تعداده («الوضع المالي للمدرسة…») فالتصق بسابقه |
| 2.2.3 | مؤشرات | 3 | 2 | «وفق خصائص…» تكملةٌ عبر فاصل صفحة، وانتهى الشقّ الأوّل بنقطة شاردة |
| 3.1.2 | مؤشرات | 5 | 4 | «الطلبة، وتحليل…» تكملةٌ عبر فاصل صفحة كُرِّرت عندها كلمة «الطلبة» |
| 3.1.4 | أوصاف | 4 | 2 | شظيّتان عبر فاصل صفحة («لمستوى تحصيلهم…» · «أو يظهر…») حُسبتا وصفين |
| 3.1.5 | أوصاف | 2 | 1 | شظيّة تملأ مستوىً واحداً («أقل من المعدلات…») حُسبت وصفاً |
| 5.3.2 | أوصاف | 8 | 7 | شظيّة تملأ مستوىً واحداً («والملائم في هذه الحالات…») حُسبت وصفاً |

**ولم تُصحَّح المواضع الستّة بتثبيت أعدادها،** بل بإصلاح القاعدة التي أخطأت —
ثم يُعاد الحساب على المعايير الـ73 كلّها:

- **الشظيّة** تُعرَف ببصمة لا بالترقيم: لا مستوى فيها يبدأ بكلمة تفتح وصفاً، ومعها
  ابتداءٌ بواو/أو/حرف جرّ أو صفٌّ شحيح. فالشظايا الأربع كان سابقها منتهياً بنقطة،
  والقاعدة القديمة اشترطت غياب النقطة.
- **حدّ الخليّة** في المؤشرات صار يُفحص أيضاً حين يبدأ الشقّ التالي كتكملة أو تتكرّر
  الكلمة عند الحدّ — لا حين يغيب النقطة وحده.
- **بندٌ بلا علامة** داخل خليّة معلَّمة: سطرٌ جديد بعد جملة تامّة ولا يبدأ كتكملة.

**وصار جدولك مرجعاً ملزِماً:** حُفظ في \`work/audit_counts.json\`، و\`verify.mjs\` يُخفق
إن اختلف أيّ عدد في أيّ معيار فرعي. فلا يمرّ إصلاحٌ مستقبليّ يُفسد موضعاً آخر بصمت.

### تعديلٌ على النصّ الحرفيّ — اعتمده المستخدم (2026-09-14)

${textEdits.map(e => `- **${e.key.replace('@', ' — المؤشر ')}**: الوثيقة تكرّر كلمةً عند حدّ الصفحة
  (\`${esc(e.source)}\`). وُصِل الشقّان بحذف التكرار والنقطة الشاردة، **واعتمد المستخدم النصّ المصحَّح** بدل نصّ الوثيقة الحرفيّ:
  «${esc(e.text)}»`).join('\n')}

## 1) ما استُخرج

| المستوى | العدد |
|---|---|
| معيار رئيس | ${c.standards.length} |
| جانب | ${c.aspects.length} |
| معيار فرعي | ${c.subs.length} |
| مؤشر أداء | ${totalInd} |
| **الإجمالي** | **${c.standards.length + c.aspects.length + c.subs.length + totalInd}** |

ومعها لكل معيار فرعي: **البيانات التوضيحية** و**أسئلة التأمل الذاتي** كما وردت في الوثيقة.

**التحقق الآلي** (\`node scripts/qnsa-extract/verify.mjs\`) بثلاثة فحوص مستقلّة:

| الفحص | يُثبت | النتيجة |
|---|---|---|
| المطابقة الحرفية | أن النصّ من الوثيقة | ${NODES - EDITS} من ${NODES} عقدة حرفياً؛ والباقي ${EDITS} تعديلان بقرار صريح (3.1.8 · 3.1.2) |
| التغطية | ألّا يضيع نصّ | عمود المؤشرات كلّه في الكتالوج (المفقود عنوانان وكلمة مكرّرة عند حدّ صفحة) |
| **المرجع البشريّ** | **أن التقسيم صحيح** | عدد المؤشرات والأوصاف يطابق تدقيقك اليدويّ في **73 من 73** |

والفحص الثالث هو الحاسم: المطابقة الحرفية تشهد أن كل عبارة من الوثيقة، لكنها **لا تشهد**
أنها قُسِّمت صحيحاً — فبندان مدموجان يطابقان الوثيقة حرفياً كبندين منفصلين. انظر §0.

## 2) ✅ خطأ ترقيم في الوثيقة — عولج

«الإدارة الصفية تسهم في توفير بيئة داعمة لتعلم الطلبة.» ترد في **جدول الملخّص برقم 2.3.4**
وفي **قسم التفاصيل برقم 2.3.3**.

**قرار المستخدم (2026-09-10):** يُعتمد **2.3.3** ويُوحَّد في الكتالوج كلّه.
النتيجة: الجانب 2.3 = **2.3.1 · 2.3.2 · 2.3.3** (تسلسل متصل)، ومؤشرات قسم التفاصيل الثلاثة مربوطة به.

## 3) ✅ صياغات المعايير الفرعية — حُسمت كلّها

صاغت الوثيقة 21 معياراً فرعياً بشكلين مختلفين بين **جدول الملخّص** و**قسم التفاصيل**
(وكان يُعدّ 22 قبل اكتشاف أن فرق 5.1.2 وهمٌ استخراجيّ — انظر أدناه). حُسمت على دفعتين.

### 3.أ الدفعة الأولى — 7 بصياغة معتمدة (2026-09-10)

النصّ المعتمد أدناه يُطبَّق في **الملخّص والتفاصيل معاً**:

| الكود | النصّ المعتمد | كان في الملخّص |
|---|---|---|
${c.subs.filter(s => s.decidedOn === '2026-09-10' && s.summaryName)
  .map(s => `| ${s.code} | ${esc(s.name)} | ${esc(s.summaryName)} |`).join('\n')}

> **ملاحظة على 3.1.8:** النصّ المعتمد صاغه المستخدم ولا يرد بهذا الشكل في الوثيقة
> (الملخّص: «…لتحديد مستوى الأداء وتطويره.» · التفاصيل: «…لتحديد مستوى الأداء، والاستفادة منها.»).
> فهو أحد **استثناءين** من قاعدة المطابقة الحرفية، وكلاهما بقرار المستخدم الصريح
> (والآخر وصل 3.1.2 بحذف كلمة مكرّرة — انظر §0).

### 3.ب الدفعة الثانية — 14 باختيار إحدى صياغتي الوثيقة (2026-09-15)

اختار المستخدم لكلٍّ منها إحدى الصياغتين **كما وردت حرفياً**، فبقيت كلّها داخل المطابقة الحرفية.
طُبِّقت على القاعدة المثبَّتة بالترحيل **066** (تحديث \`name_ar\` وحده، دون إعادة تشغيل 064).

| الكود | المعتمد | الصياغة الأخرى | الاختيار |
|---|---|---|---|
${c.subs.filter(s => s.decidedOn === '2026-09-15')
  .map(s => {
    const other = s.wordingPick === 'detail' ? s.summaryName : s.detailName
    return `| ${s.code} | ${esc(s.name)} | ${esc(other)} | ${s.wordingPick === 'detail' ? 'التفاصيل' : 'الملخّص'} |`
  }).join('\n')}

- **3.1.6** يحمل تصحيحاً إملائياً فعلياً: الملخّص يكتب «TIMMS» والصحيح «**TIMSS**» كما في التفاصيل.
- **5.1.2 لم يكن فرقاً:** اسمه في قسم التفاصيل قُطِع عند حدّ صفحة («تدريب وتأهيل الموظفين الجدد»
  ‖ «لضمان أداء مهامهم بكفاءة وجودة.») فبدا مبتوراً. عولج الاستخراج فصارت الصياغتان متطابقتين.

**المعلّق الآن: ${c.subs.filter(s => !s.decided && s.detailName && s.detailName !== s.name).length}.**

## 4) ✅ ${merges.length + flags.filter(f => f.kind.startsWith('حدّ صفحة')).length} موضعاً عند حدود الصفحات — حُسمت

انقطع نصّ «مؤشرات الأداء» بين خليّتين عبر فاصل صفحة. **لم يُوصَل أي منها تخميناً**؛
القرار مسجَّل صريحاً لكل موضع في خريطة \`JUNCTION\` داخل \`scripts/qnsa-extract/build_catalog.mjs\`.
تسعةٌ منها حُسمت في 2026-09-12، واثنان (2.2.3 · 3.1.2) كشفهما تدقيقك اليدويّ (§0).

### 4.أ ${merges.length} وُصِلت في مؤشر واحد

المعيار لغوي لا تقديري: الشقّ الثاني يبدأ بكلمة لا تصحّ بداية جملة (حرف جرّ · عطف ·
صفة · مضاف إليه) أو يكرّر آخر كلمة قبله. وقد **أكّد التحقّق الآلي** الوصل: النصّ الموصول
يرد متّصلاً في مجرى العمود نفسه داخل الوثيقة (عدا 3.1.2 المعدَّل بقرار).

| # | المؤشر | النصّ بعد الوصل |
|---|---|---|
${merges.map((m, i) => `| ${i + 1} | ${m.key.replace('@', '.')} | ${esc(m.text)} |`).join('\n')}

### 4.ب ${flags.filter(f => f.kind.startsWith('حدّ صفحة')).length} بقيت منفصلة

| المعيار الفرعي | نهاية البند السابق | بداية البند التالي | السبب |
|---|---|---|---|
${flags.filter(f => f.kind.startsWith('حدّ صفحة'))
  .map(f => `| ${f.code} | …${esc(f.before || '')} | ${esc(f.after || '')}… | جملة مستقلّة تامّة |`).join('\n')}

## 5) توزيع المؤشرات

| الجانب | معايير فرعية | مؤشرات |
|---|---|---|
${c.aspects.map(a => {
  const subs = c.subs.filter(s => s.parent === a.code)
  return `| ${a.code} ${esc(a.name)} | ${subs.length} | ${subs.reduce((n, s) => n + s.indicators.length, 0)} |`
}).join('\n')}

## 6) ✅ سلم التقدير اللفظي — استُخرج (ترحيل 065)

**${RUB.descCount} وصفاً** لـ73 معياراً فرعياً × 5 مستويات = **${RUB.phrases} عبارة وصفية**،
من 42 جدولاً بستة أعمدة. **التحقّق: ${RUB.phrases}/${RUB.phrases} مطابقة حرفياً — صفر اختلاف.**

| المستوى | العبارات |
|---|---|
${[1, 2, 3, 4, 5].map(n => `| ${RUB.levels[n]} | ${RUB.byLevel[n] || 0} |`).join('\n')}

### 6.أ ‼ قرار نمذجة: السلم يُربَط بالمعيار الفرعي لا بالمؤشر

الوثيقة تضع **${RUB.descCount} وصفاً مقابل ${totalInd} مؤشراً**، ويتساوى العدد في
**${RUB.exact} معياراً فرعياً من 73** فقط. فالوصف قد يجمع مؤشّرين أو يفصّل واحداً —
و**3.1.6** خير مثال: مؤشران، وأربعة أوصاف (تهيئة · تيمز · بيرلز · بيزا).
لذلك ربطناه بعقدة **المستوى الثالث** مع \`row_index\`؛ وربطه بالمؤشرات واحداً لواحد
كان سيختلق علاقةً لا تقولها الوثيقة.

### 6.ب الوصف المقسوم على صفّين — كيف مُيِّز من وصفٍ جديد

هذه أخطر علّة في السلم: وصفٌ يُقطَع عند حدّ صفحة فيُحسَب وصفين. **وغياب النقطة
الختامية ليس دليلاً** — الوثيقة تُسقطها كثيراً. فالوصل لا يحدث إلا بشاهد موجَب:

| الشاهد | طبيعته |
|---|---|
| فاصل صفحة مسجَّل في الوثيقة (\`lastRenderedPageBreak\`) | مادّي، قاطع |
| الوصف السابق ينتهي بحرف جرّ أو عطف أو فاصلة | نحويّ |
| أغلب مستويات الصفّ لا تبدأ بكلمة «تفتح وصفاً» | نحويّ |

وقائمة ما «يفتح وصفاً» **مستخرجة من الوثيقة نفسها**: الصفوف التي تحمل كود معيارٍ
فرعيّ أوصافٌ جديدة يقيناً، وكلماتها الأولى 38 كلمة ليس فيها ما يبدأ بواو إلا «وحدة».
فالبدء بواو شاهد تكملة موثوق.

| العلّة | المعالجة |
|---|---|
| ترتيب الأعمدة ينعكس بين الجداول (المعيار في 0 أو 5، والسلم تصاعدي أو تنازلي) | يُقرأ من صفّ الرأس |
| 3 جداول بلا صفّ رأس — ولا ترث ترتيب سابقها لأن الشكل ينقلب فعلاً | يُستنتَج من عمود الأكواد |
| «جيد جداً» / «جيد جدا» | تطبيع التشكيل والألف والياء |
| صفوف الرأس وعناوين الجوانب تتكرّر عند كل فاصل صفحة | تُستبعَد من البيانات |
| الوصف يُقطَّع على صفّين أو ثلاثة | وُصِل **${RUB.merged}** موضعاً بشاهد موجَب |

### 6.ج ما يحتاج عينك

- **${RUB.votes} موضعاً** شاهده نحويّ لا مادّي (وصل ${RUB.votesWrap} · فصل ${RUB.votesSplit}) — في \`work/rubric_flags.json\` تحت \`votes\`.
- **${RUB.unterminated} وصفاً لا ينتهي بعلامة جملة** — فُحِص كلٌّ منها: إما لا وصف بعده، أو
  الوصف التالي يفتتح بفعل صريح. فهي سقوط نقطة في الوثيقة لا انقسامٌ فائت.
- **${RUB.missing} وصفاً ناقص مستوى أو أكثر** — خلايا خالية في الوثيقة (أبرزها «مقبول»).
- **${RUB.over} معايير فرعية أوصافها أكثر من مؤشراتها** — فُحِصت يدوياً وكلّها أوصاف
  متمايزة فعلاً لا أوصافٌ وهمية.

ولتصحيح أيّ موضع: ضَع في \`scripts/qnsa-extract/rubric_junctions.json\` مفتاحاً
بصيغة \`"الكود#فهرس الصفّ": "wrap"\` أو \`"split"\` ثم أعد التوليد. القرار البشريّ
يغلب كلّ الشواهد.

### 6.د عدد الأوصاف لكل معيار فرعي — للمراجعة بالعين

| المعيار | أوصاف | مؤشرات | | المعيار | أوصاف | مؤشرات |
|---|---|---|---|---|---|---|
${(() => {
  const rows = rflags.report.map(r => `${r.code} | ${r.rubricRows} | ${r.indicators ?? '—'}`)
  const half = Math.ceil(rows.length / 2)
  return rows.slice(0, half).map((l, i) =>
    `| ${l} | | ${rows[half + i] || ' | | '} |`).join('\n')
})()}

## 7) ✅ نماذج الأدلة والوثائق — استُخرجت (ترحيل 067)

**${EV.total} دليلاً** في 15 جدولاً، جدولٌ لكل جانب. **التحقّق: ${EV.total}/${EV.total} نصّها يرد في الوثيقة حرفياً.**

### 7.أ ‼ قرار نمذجة: الدليل يُربَط بالجانب لا بالمعيار الفرعي

الوثيقة تضع قائمة أدلة **واحدة لكل جانب**، ولا تُسند الدليل إلى معيار فرعي أو مؤشر بعينه.
لذلك رُبط بعقدة **المستوى الثاني**. وكثير من الأدلة يتكرّر بين الجوانب بالنصّ نفسه
(«بطاقة تقرير الأداء المدرسي» · «محاضر الاجتماعات / المراسلات» · «تحليل نتائج الاستبانات
واستطلاعات الرأي») — حُفظ كما ورد في كل جانب، وتوحيده في مكتبة أنواع أدلة قرارٌ لاحق.

### 7.ب ‼ جدول أدلة 2.1 كان ضائعاً — والسبب في المحلّل لا في الوثيقة

الوثيقة فيها **فقرة فارغة واحدة مغلقة ذاتياً** (\`<w:p …/>\`)، قبل جدول أدلة 2.1 مباشرة. عدّها
المحلّل فقرةً مفتوحة فتخطّى كل ما بعدها حتى أوّل \`</w:p>\` — فابتلع وسوم فتح الجدول. فكان في
الوثيقة **90** جدولاً وفي المحلَّل **89**.
أُصلح المحلّل، وصار **يتوقّف بخطأ صريح إن اختلف عدد الجداول المحلَّلة عن وسوم \`<w:tbl>\`**.
وأُعيد بناء الكتالوج والسلم بعد الإصلاح فجاءا **مطابقين بايتاً ببايت** لما قبله.

### 7.ج علل الجداول وكيف عُولجت

| العلّة | المعالجة | مواضعها |
|---|---|---|
| ترتيب القراءة ينعكس بين الجداول | \`<w:bidiVisual/>\` يحدّد الخليّة اليمنى (تُحقِّق منه على 39 جدول سلم: 39/39) | كلّها |
| أدلّة عدّة ملصقة في سطر واحد | تُفصَل عند شرطةٍ تلي علامة نهاية جملة فقط | ${EV.splits} |
| دليل ملتفّ على سطرين أو ثلاثة | يُوصَل إن كان سابقه غير منتهٍ أو فيه قوس مفتوح | ${EV.joins} (كلّها في 2.1) |
| دليل سقطت شرطته | سطرٌ بلا شرطة بعد دليلٍ تامّ = دليلٌ مستقلّ | ${EV.lost} |
| خلايا بلا علامات ولا نقاط (1.1 · 4.1) | كل سطر دليل | — |

### 7.د عدد الأدلة لكل جانب — للمراجعة بالعين

| الجانب | الأدلة | | الجانب | الأدلة | | الجانب | الأدلة |
|---|---|---|---|---|---|---|---|
${(() => {
  const g = evid.groups
  const cell = x => x ? `${x.code} ${esc(catalog_aspect(x.code))} | ${x.items.length}` : ' | '
  const out = []
  for (let i = 0; i < 5; i++) out.push(`| ${cell(g[i])} | | ${cell(g[i + 5])} | | ${cell(g[i + 10])} |`)
  return out.join('\n')
})()}

## 8) ما لم يُستخرج بعد

- **الأسماء الإنجليزية** للعقد — الوثيقة عربية فقط؛ عمود \`name_en\` جاهز وينتظر النسخة الإنجليزية الرسمية.
`

fs.writeFileSync(path.join(ROOT, 'docs/QNSA_CATALOG_REVIEW.md'), md, 'utf8')
console.log('كُتب تقرير المراجعة:', Math.round(md.length / 1024), 'KB')
